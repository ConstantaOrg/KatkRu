# 🏫 KatkRu

![Python](https://img.shields.io/badge/Python-3.12.8-3776AB?style=flat&logo=python&logoColor=white)
![FastAPI](https://img.shields.io/badge/FastAPI-0.115.0-009688?style=flat&logo=fastapi&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-4169E1?style=flat&logo=postgresql&logoColor=white)
![Elasticsearch](https://img.shields.io/badge/Elasticsearch-9.0.2-005571?style=flat&logo=elasticsearch&logoColor=white)
![Redis](https://img.shields.io/badge/Redis-7.4-DC382D?style=flat&logo=redis&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-2.Х-2496ED?style=flat&logo=docker&logoColor=white)
![Nginx](https://img.shields.io/badge/Nginx-1.28.0-009639?style=flat&logo=nginx&logoColor=white)
![Grafana](https://img.shields.io/badge/Grafana-11.0.0-F46800?style=flat&logo=grafana&logoColor=white)
![Test Coverage](https://img.shields.io/badge/Coverage-91%25-brightgreen?style=flat&logo=pytest&logoColor=white)


## 📋 Описание

KatkRu - это система для автоматизации процесса составления и управления расписанием занятий в учебных заведениях. Система предоставляет веб-интерфейс для методистов, позволяющий импортировать расписание из документов Word, редактировать его в визуальном редакторе и публиковать для студентов и преподавателей.

## ✨ Ключевые возможности

- **Импорт расписания из DOCX файлов** - автоматический парсинг расписания с поддержкой числителя/знаменателя
- **Веб-редактор расписания** - интуитивный интерфейс для создания и редактирования расписания
- **Версионирование расписания и анализ конфликтов** - отслеживание изменений, выявление конфликтов преподавателей и аудиторий
- **Полнотекстовый поиск** - быстрый поиск по специальностям, группам, преподавателям и дисциплинам через Elasticsearch
- **Управление пользователями** - добавление и изменение пользователей через защищённые `/server` эндпоинты
- **Мониторинг и логирование** - сбор метрик производительности и логов для анализа в Grafana
- **Защита от атак** - автоматический бан через Nginx + iptables на основе анализа логов

## 👥 Пользователи системы

- **Методисты** - создают, редактируют и утверждают расписание
- **Студенты** - просматривают актуальное расписание для своей группы

## 🏗️ Архитектура

### Структура проекта

```
KatkRu/
├── core/                      # Ядро приложения
│   ├── api/                   # API эндпоинты
│   │   ├── elastic_search/    # Поиск через Elasticsearch
│   │   ├── n8n_ui/            # UI редактор расписания
│   │   ├── ttable_versions/   # Управление версиями расписания
│   │   ├── users/             # Управление пользователями
│   │   ├── timetable_api.py   # Публичный API расписания
│   │   ├── groups_tab.py      # Управление группами
│   │   ├── teachers_tab.py    # Управление преподавателями
│   │   ├── disciplines_tab.py # Управление дисциплинами
│   │   ├── specialties.py     # Управление специальностями
│   │   └── middleware.py      # Middleware (Auth, Logging)
│   ├── config_dir/            # Конфигурация приложения
│   ├── data/                  # Слой данных (PostgreSQL, Redis)
│   ├── schemas/               # Pydantic схемы запросов
│   ├── response_schemas/      # Pydantic схемы ответов
│   ├── utils/                 # Утилиты (JWT, логирование, мониторинг)
│   └── main.py                # Точка входа приложения
├── docs/                      # Документация модулей
│   ├── api/                   # Документация API эндпоинтов
│   └── utils/                 # Документация утилит
├── tests/                     # Тесты
│   ├── unit/                  # Юнит-тесты
│   ├── integrate/             # Интеграционные тесты
│   └── e2e/                   # End-to-end тесты
├── autoban_iptables/          # Скрипты автобана через iptables
├── grafana/                   # Конфигурация Grafana
├── secrets/                   # Секреты (сертификаты, ключи, дампы БД)
├── logs/                      # Логи приложения
├── docker-compose.yml         # Основной Docker Compose
├── docker-compose.security.yml # Docker Compose для security контейнера
├── nginx.conf                 # Конфигурация Nginx
└── requirements.txt           # Python зависимости
```

### Архитектурные паттерны

**Layered Architecture (Слоистая архитектура):**
- **API Layer** (`core/api/`) - обработка HTTP запросов, валидация, маршрутизация
- **Business Logic Layer** (`core/api/`) - бизнес-логика в эндпоинтах
- **Data Access Layer** (`core/data/`) - работа с БД через репозитории
- **Schemas Layer** (`core/schemas/`, `core/response_schemas/`) - валидация данных

**Repository Pattern:**
- Абстракция работы с БД через классы-репозитории
- Изоляция SQL запросов от бизнес-логики
- Примеры: `TimetableQueries`, `UsersQueries`, `GroupsQueries`

**Dependency Injection:**
- FastAPI `Depends()` для инжекции зависимостей
- Переиспользуемые dependencies: `PgSqlDep`, `ElasticDep`, `JWTCookieDep`

**Middleware Pattern:**
- `ASGILoggingMiddleware` - логирование всех запросов
- `AuthUXASGIMiddleware` - аутентификация через JWT
- `CORSMiddleware` - обработка CORS

**Factory Pattern:**
- `issue_aT_rT()` - создание пары JWT токенов
- Response factories в `response_model_utils.py`

## 🔑 Ключевые компоненты

### 1. JWT Аутентификация
- Access token (короткий срок жизни) для доступа к API
- Refresh token (длинный срок жизни) для обновления access token
- HTTP-only cookies для безопасного хранения токенов
- Отслеживание сессий с привязкой к устройствам (user-agent, IP)

### 2. Rate Limiting
- Защита от брутфорс атак на эндпоинт `/login`
- Ограничение: 5 попыток за 5 минут с одного IP
- Хранение счётчиков в Redis с автоматическим TTL

### 3. Парсинг DOCX расписаний
- Автоматическое извлечение данных из документов Word
- Поддержка числителя/знаменателя (чередование по неделям)
- Извлечение ФИО преподавателей через regex
- Нормализация данных для загрузки в БД

### 4. Версионирование расписания
- Создание версий расписания (standard/replacements)
- Статусы: pending (в ожидании), accepted (утверждено)
- Флаг `is_commited` для активной версии
- Изоляция версий по корпусам учебного заведения

### 5. Полнотекстовый поиск
- Elasticsearch для быстрого поиска
- Индексы: специальности, группы, преподаватели, дисциплины
- Режимы поиска: autocomplete (edge n-gram), deep (fuzzy search)
- Батч-индексация для оптимизации производительности

### 6. Мониторинг и логирование
- Структурированные логи в JSON формате
- Отправка логов в Elasticsearch через Filebeat
- Мониторинг CPU/RAM контейнера
- Визуализация метрик в Grafana
- ILM политика для автоматической ротации логов (90 дней)

### 7. Nginx + Автобан
- Разграничение запросов: `/api/public/`, `/api/private/`, `/api/server/`
- Rate limiting на уровне Nginx (10 r/s для публичных эндпоинтов)
- Автоматический анализ логов и бан через iptables
- Whitelist для внутренних IP адресов

## 🛡️ Безопасность

### Nginx маршрутизация

**Geo и Map конфигурация:**
```nginx
geo $white_list {
    default         0;
    172.*.0.*       1;  # Docker networks (172.16.0.0 - 172.31.255.255)
    10.0.0.0/8      1;  # Private network
}

map $http_cookie $has_cookies {
    default 0;
    "~*access_token=.*refresh_token=.*" 1;
}

map $white_list:$has_cookies $req_limit_condition {
    0:0 "limit/no_cookies";
    0:1 "limit/to_check";
    1:1 "nolimit";
    1:0 "nolimit";
}
```

**Маршруты:**

1. **`/api/public/`** - публичные эндпоинты
   - Пропускает любой запрос без кук
   - Если куки есть, воспринимается как `/api/private/` в middleware
   - Rate limit: 10 r/s для пользователей
   - Без ограничений для whitelist IP

2. **`/api/private/`** - приватные эндпоинты
   - Требует наличие `access_token` и `refresh_token` в cookies
   - `limit/no_cookies` - запрос без кук → 401 Unauthorized
   - `limit/to_check` - запрос с куками → проверка в `AuthUXMiddleware`
   - `nolimit` - серверное обращение из whitelist

3. **`/api/server/`** - серверные эндпоинты
   - Доступ только с whitelist IP адресов
   - `block` - запрос пользователя → 401 (не 403, чтобы не раскрывать наличие эндпоинта)
   - `pass` - запрос сервера → без лимитов и проверки кук

### Автобан через iptables

Bash скрипты в `autoban_iptables/` анализируют логи Nginx и автоматически блокируют подозрительные IP:
- Обнаружение брутфорс атак
- Блокировка через iptables
- Логирование заблокированных IP
- Управление списком банов

## 🚀 Технологии

### Backend
- **Python 3.12** - язык программирования
- **FastAPI** - асинхронный веб-фреймворк
- **Pydantic** - валидация данных
- **asyncpg** - асинхронный драйвер PostgreSQL
- **python-docx** - парсинг DOCX файлов
- **PyJWT** - работа с JWT токенами
- **argon2-cffi** - хеширование паролей
- **psutil** - мониторинг ресурсов

### Базы данных и хранилища
- **PostgreSQL** - основная реляционная БД
- **Elasticsearch** - полнотекстовый поиск и хранение логов
- **Redis** - кэширование и rate limiting

### Инфраструктура
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация контейнеров
- **Nginx** - reverse proxy, rate limiting, маршрутизация
- **iptables** - сетевой фаервол для автобана

### Мониторинг и логирование
- **Filebeat** - сбор логов из файлов
- **Kibana** - просмотр логов
- **Grafana** - визуализация метрик
- **colorlog** - цветные логи в консоли

### Тестирование
- **pytest** - фреймворк для тестирования
- **pytest-asyncio** - поддержка async тестов
- **hypothesis** - property-based testing
- **httpx** - HTTP клиент для тестов

## 📦 Установка и запуск

### Требования

- Docker и Docker Compose
- Python 3.12+ (для локальной разработки)
- Git

### Быстрый старт

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd KatkRu
```

2. **Настройка переменных окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками и подготовьте 2 файла
# 1. .env.prod
# 2. .env.test
```

3. **Запуск приложения:**
```bash
docker-compose -f docker-compose.security.yml -f docker-compose.yml up -d
```

**Важно:** Security контейнер должен быть запущен первым, иначе основной контейнер не сможет использовать режим сети Nginx.

4. **Проверка статуса:**
```bash
docker-compose ps
```

5. **Доступ к сервисам:**
- API документация: http://localhost/api/docs
- Kibana: http://localhost:5601
- Grafana: http://localhost:3000

### Инициализация Elasticsearch

При первом запуске установите переменную окружения:
```bash
ES_INIT=1
```

Это создаст индексы и загрузит данные из PostgreSQL.

### Локальная разработка

1. **Создание виртуального окружения:**
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows
```

2. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

3. **Запуск приложения:**
```bash
uvicorn core.main:app
```

## 🧪 Тестирование

### Запуск всех тестов
```bash
pytest
```

### Запуск с покрытием
```bash
pytest --cov=core --cov-report=html
```

### Запуск конкретного типа тестов
```bash
pytest tests/unit/          # Юнит-тесты
pytest tests/integrate/     # Интеграционные тесты
pytest tests/e2e/           # End-to-end тесты
```

### Property-based тесты
Проект использует Hypothesis для property-based testing:
```bash
pytest tests/ -k "property"
```

## 📚 Документация модулей

Подробная документация всех модулей находится в директории `docs/`:

### API модули
- [Timetable API](docs/api/timetable_api.md) - публичный API расписания
- [Users API](docs/api/users_api.md) - управление пользователями и аутентификация
- [Groups Tab](docs/api/groups_tab.md) - управление группами
- [Teachers Tab](docs/api/teachers_tab.md) - управление преподавателями
- [Disciplines Tab](docs/api/disciplines_tab.md) - управление дисциплинами
- [Specialties](docs/api/specialties.md) - управление специальностями
- [N8N UI Main UI](docs/api/n8n_ui_main_ui.md) - главный UI редактора
- [N8N UI Ext Card](docs/api/n8n_ui_ext_card.md) - расширенная карточка расписания
- [Middleware](docs/api/middleware.md) - middleware для логирования и аутентификации
- [Elasticsearch API](docs/api/elastic_search_api_elastic_search.md) - поиск через Elasticsearch
- [Elasticsearch Sub Handlers](docs/api/elastic_search_sub_handlers.md) - батч-индексация
- [Timetable Parser](docs/api/ttable_versions_ttable_parser.md) - парсинг DOCX расписаний
- [Timetable Versions](docs/api/ttable_versions_ttable_versions_tab.md) - управление версиями
- [Rate Limiter](docs/api/users_rate_limiter.md) - защита от брутфорса

### Утилиты
- [JWT Factory](docs/utils/jwt_factory.md) - создание и валидация JWT токенов
- [Logger](docs/utils/logger.md) - структурированное логирование
- [Anything](docs/utils/anything.md) - константы, статусы, роли
- [Lite Dependencies](docs/utils/lite_dependencies.md) - проверка ролей и доступа
- [Schemas to Depends](docs/utils/schemas2depends.md) - схемы пагинации
- [Resource Monitor](docs/utils/resource_monitor.md) - мониторинг ресурсов контейнера

## 🔧 Конфигурация

### Переменные окружения

Основные переменные в `.env`:

```bash
# База данных
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=katkru
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password

# Elasticsearch
ELASTIC_VERSION=9.0.2
ELASTIC_PASSWORD=your_password
ELASTICSEARCH_HOSTS=http://es01:9200

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_password


# Приложение
ENVIRONMENT=local  # local, docker, prod
DOMAIN=http://localhost
ES_INIT=false  # true для первого запуска
UVI_WORKERS=1  # количество uvicorn workers
```

### Режимы окружения

Приложение поддерживает три режима:
- `local` - локальная разработка
- `docker` - запуск в Docker
- `prod` - production окружение

Конфигурация для каждого режима в `core/config_dir/env_modes.py`.

## 📊 Мониторинг

### Grafana дашборды

Рекомендуемые метрики для мониторинга:
- CPU и RAM контейнера
- Количество запросов в секунду
- Время ответа эндпоинтов
- Количество ошибок (4xx, 5xx)
- Количество заблокированных IP (rate limit, автобан)

### Логи

Логи доступны в:
- **Файлы:** `logs/app.log` (JSON формат)
- **Elasticsearch:** индекс `applogsindex-*`
- **Kibana:** http://localhost:5601

Структура лога:
```json
{
  "@timestamp": "2024-03-15T10:30:00Z",
  "level": "INFO",
  "message": "Request processed",
  "method": "POST",
  "url": "/api/v1/timetable/get",
  "http_status": 200,
  "response_time": 0.045,
  "ip": "192.168.1.100",
  "user_id": 5
}
```

## 🤝 Контакты

Для вопросов, предложений или согласования использования проекта:

**Email:** dev.constanta@gmail.com

## 📄 Лицензия

Проект не предназначен для свободного использования. Использование возможно только после согласования с автором проекта.

## 🚧 Статус проекта

**В разработке** - проект активно развивается, возможны breaking changes.

---

**KatkRu** © 2024-2026
