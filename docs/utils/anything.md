# 🔧 Anything (Константы и утилиты)

## Описание
Центральный модуль с константами, dataclass'ами и вспомогательными функциями, используемыми по всему приложению. Содержит определения статусов, ролей, типов и утилитные функции для работы с логами и IP адресами.

## Константы

### 🖼️ `default_avatar`
Путь к аватару пользователя по умолчанию.

```python
default_avatar = '/users/avatars/default_picture.png'
```

---

### 📊 `accept_card_constraint`
Ограничение для принятия карточек расписания.

```python
accept_card_constraint = 2
```

**Использование:**
- Минимальное количество чего-то для принятия карточки
- Требует уточнения контекста использования

## Dataclass константы

### 🎫 `TokenTypes`
Типы JWT токенов в системе.

```python
@dataclass
class TokenTypes:
    access_token: str = 'aT'   # Access token
    refresh_token: str = 'rT'  # Refresh token
    ws_token: str = 'wT'       # WebSocket token
```

**Также доступен словарь:**
```python
token_types = {
    'access_token': 'aT',
    'refresh_token': 'rT',
    'ws_token': 'wT'
}
```

**Использование:**
- Идентификация типа токена в JWT payload
- Различная логика для разных типов токенов

---

### 📅 `TimetableTypes`
Типы расписания.

```python
@dataclass
class TimetableTypes:
    standard: str = 'standard'      # Стандартное расписание
    replaces: str = 'replacements'  # Замены
```

**Описание:**
- `standard` - шаблон расписания, действующий по умолчанию
- `replacements` - замены на конкретную дату

**Использование:**
- Фильтрация версий расписания
- Определение типа при создании версии

---

### ✅ `TimetableVerStatuses`
Статусы версий расписания.

```python
@dataclass
class TimetableVerStatuses:
    accepted: int = 1  # Утверждено
    pending: int = 2   # В ожидании
```

**Описание:**
- `accepted (1)` - версия прошла проверку и готова к использованию
- `pending (2)` - версия создана, но требует проверки методистом

**Использование:**
- Фильтрация версий по статусу
- Установка статуса при импорте/утверждении
- Отображение статуса в UI

---

### 📝 `CardsStatesStatuses`
Статусы карточек расписания.

```python
@dataclass
class CardsStatesStatuses:
    accepted: int = 1  # Утверждено
    edited: int = 2    # Редактировано
    draft: int = 3     # Не трогали / по стандартному расписанию
```

**Описание:**
- `accepted (1)` - карточка утверждена методистом
- `edited (2)` - карточка была изменена, требует проверки
- `draft (3)` - карточка не редактировалась, взята из стандартного расписания

**Использование:**
- Отслеживание изменений в карточках
- Валидация перед утверждением версии
- Подсветка изменённых карточек в UI

---

### 👤 `Roles`
Роли пользователей в системе.

```python
@dataclass
class Roles:
    methodist: str = 'methodist'  # Методист (полный доступ)
    read_all: str = 'read_all'    # Только чтение
```

**Описание:**
- `methodist` - может создавать, редактировать и утверждать расписание
- `read_all` - может только просматривать данные

**Использование:**
- Проверка прав доступа в `role_require()`
- Установка роли при регистрации
- Фильтрация функционала в UI

---

### 📦 `ModuleNames`
Константы для названий модулей (используется в генераторе документации).

```python
@dataclass
class ModuleNames:
    specialties: str = 'specialties'
    groups: str = 'groups'
    teachers: str = 'teachers'
    disciplines: str = 'disciplines'
    timetable: str = 'timetable'
    users: str = 'users'
    n8n_ui: str = 'n8n_ui'
    elastic_search: str = 'elastic_search'
    ttable_versions: str = 'ttable_versions'
```

**Использование:**
- Генерация документации API
- Идентификация модулей в коде
- Единообразное именование

---

### 🌐 `HttpMethods`
Константы для HTTP методов.

```python
@dataclass
class HttpMethods:
    GET: str = 'GET'
    POST: str = 'POST'
    PUT: str = 'PUT'
    DELETE: str = 'DELETE'
    PATCH: str = 'PATCH'
```

**Использование:**
- Генерация документации
- Валидация HTTP методов
- Типизация в коде

---

### 📍 `ParameterLocations`
Константы для расположения параметров в HTTP запросе.

```python
@dataclass
class ParameterLocations:
    query: str = 'query'    # Query параметры (?key=value)
    path: str = 'path'      # Path параметры (/users/{id})
    header: str = 'header'  # HTTP заголовки
    body: str = 'body'      # Тело запроса
```

**Использование:**
- Генерация OpenAPI документации
- Парсинг параметров запросов
- Валидация расположения параметров

---

### 🔤 `FieldTypes`
Константы для типов полей в схемах.

```python
@dataclass
class FieldTypes:
    string: str = 'string'
    integer: str = 'integer'
    number: str = 'number'
    boolean: str = 'boolean'
    array: str = 'array'
    object: str = 'object'
```

**Использование:**
- Генерация OpenAPI схем
- Валидация типов данных
- Документирование API

---

### 📂 `ModulePaths`
Пути к модулям для анализа зависимостей.

```python
@dataclass
class ModulePaths:
    elastic_search: str = "core.api.elastic_search"
    n8n_ui: str = "core.api.n8n_ui"
    ttable_versions: str = "core.api.ttable_versions_tab"
    timetable: str = "core.api.timetable.timetable_api"
    users: str = "core.api.users.users_api"
```

**Использование:**
- Импорт модулей динамически
- Анализ зависимостей между модулями
- Генерация документации

## Функции

### 🔒 `hide_log_param()`
Скрытие части параметра в логах для безопасности.

**Сигнатура:**
```python
def hide_log_param(param: str, start: int = 3, end: int = 8) -> str
```

**Параметры:**
- `param` (str) - параметр для скрытия (email, токен и т.д.)
- `start` (int, default=3) - количество символов в начале
- `end` (int, default=8) - количество символов в конце

**Возвращает:**
- `str` - параметр с скрытой серединой

**Примеры:**
```python
hide_log_param("user@example.com")
# "use***@exa***.com"

hide_log_param("very_secret_token_12345", start=4, end=5)
# "very_***12345"

hide_log_param("short")
# "sho***ort" (может выглядеть странно для коротких строк)
```

**Использование:**
- Логирование email адресов
- Скрытие токенов в логах
- Защита от утечки данных через логи

---

### 📁 `create_log_dirs()`
Создание директории для логов.

**Сигнатура:**
```python
def create_log_dirs()
```

**Логика:**
- Создаёт директорию `logs/` в корне проекта
- Использует `exist_ok=True` - не падает, если директория существует

**Использование:**
- Вызывается при старте приложения
- Обеспечивает наличие директории для файлов логов

---

### 🌐 `get_client_ip()`
Извлечение IP адреса клиента с учётом прокси.

**Сигнатура:**
```python
def get_client_ip(request: Request | WebSocket) -> str
```

**Параметры:**
- `request` (Request | WebSocket) - объект запроса

**Возвращает:**
- `str` - IP адрес клиента

**Логика:**
```python
xff = request.headers.get('X-Forwarded-For')
ip = xff.split(',')[0].strip() if (
    xff and request.client.host in env.trusted_proxies
) else request.client.host
return ip
```

**Алгоритм:**
1. Проверяет наличие заголовка `X-Forwarded-For`
2. Если заголовок есть И запрос от доверенного прокси:
   - Берёт первый IP из списка (реальный IP клиента)
3. Иначе:
   - Берёт `request.client.host` (IP прокси или клиента)

**Примеры:**
```python
# Прямое подключение
request.client.host = "192.168.1.100"
request.headers['X-Forwarded-For'] = None
get_client_ip(request)  # "192.168.1.100"

# Через доверенный прокси
request.client.host = "10.0.0.1"  # IP прокси (в trusted_proxies)
request.headers['X-Forwarded-For'] = "192.168.1.100, 10.0.0.1"
get_client_ip(request)  # "192.168.1.100" (реальный IP)

# Через недоверенный прокси
request.client.host = "1.2.3.4"  # IP прокси (НЕ в trusted_proxies)
request.headers['X-Forwarded-For'] = "192.168.1.100"
get_client_ip(request)  # "1.2.3.4" (не доверяем заголовку)
```

**Безопасность:**
- Доверяет `X-Forwarded-For` только от `trusted_proxies`
- Предотвращает IP spoofing
- Важно правильно настроить `env.trusted_proxies`

**Конфигурация Nginx:**
```nginx
# Правильная настройка
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

# Альтернатива (закомментирована в коде)
# proxy_set_header X-Forwarded-For $remote_addr;
```

---

### 🔍 `extract_conflict_values()`
Извлечение значений из строки ошибки конфликта PostgreSQL.

**Сигнатура:**
```python
def extract_conflict_values(detail_str: str) -> list[tuple]
```

**Параметры:**
- `detail_str` (str) - строка с деталями ошибки из PostgreSQL

**Возвращает:**
- `list[tuple]` - список кортежей с конфликтующими значениями

**Логика:**
- Ищет значения в скобках `(...)`
- Разбивает по запятым
- Возвращает список кортежей

**Пример:**
```python
detail = "Key (email)=(user@example.com) already exists. Key (phone)=(+1234567890) conflicts."
extract_conflict_values(detail)
# [('email', '=user@example.com'), ('phone', '=+1234567890')]

# Типичная ошибка PostgreSQL
detail = "duplicate key value violates unique constraint (group_name)=(23И1)"
extract_conflict_values(detail)
# [('group_name', '=23И1')]
```

**Использование:**
- Парсинг ошибок уникальности из PostgreSQL
- Извлечение конфликтующих значений для логов
- Формирование понятных сообщений об ошибках

## Бизнес-логика

### Статусы расписания

**Жизненный цикл версии:**
1. Импорт из DOCX → `status_id = 2` (pending)
2. Проверка методистом → редактирование карточек
3. Утверждение → `status_id = 1` (accepted)
4. Публикация → `is_commited = true`

**Жизненный цикл карточки:**
1. Импорт → `status_id = 3` (draft)
2. Редактирование → `status_id = 2` (edited)
3. Утверждение → `status_id = 1` (accepted)

### Роли и доступ

**Иерархия (неформальная):**
- `methodist` > `read_all`
- Методист может всё, что может read_all + редактирование

**Разделение обязанностей:**
- Методист: создание, редактирование, утверждение расписания
- Read-only: просмотр данных, аналитика, отчёты

### IP адреса и безопасность

**Trusted proxies:**
- Список IP адресов прокси-серверов, которым доверяем
- Обычно: внутренние IP (10.x.x.x, 172.16.x.x, 192.168.x.x)
- Не добавлять публичные IP!

**X-Forwarded-For:**
- Заголовок с цепочкой IP адресов
- Формат: `client_ip, proxy1_ip, proxy2_ip`
- Берём первый IP (реальный клиент)

## Зависимости

### Внутренние модули
- `core.config_dir.config.env` - конфигурация приложения

### Внешние библиотеки
- `dataclasses.dataclass` - создание dataclass'ов
- `pathlib.Path` - работа с путями
- `starlette.requests.Request` - объект запроса
- `starlette.websockets.WebSocket` - WebSocket соединение

### Использование в модулях

Модуль используется практически везде:
- Все API модули - статусы, роли
- Middleware - `get_client_ip()`
- Logger - `hide_log_param()`
- Dependencies - `Roles`
- И многие другие

## Примеры использования

### Использование статусов
```python
from core.utils.anything import TimetableVerStatuses, CardsStatesStatuses

# Создание версии в ожидании
ttable_version = {
    "type": "standard",
    "status_id": TimetableVerStatuses.pending,
    "is_commited": False
}

# Проверка статуса карточки
if card['status_id'] == CardsStatesStatuses.edited:
    print("Карточка была изменена")
```

### Использование ролей
```python
from core.utils.anything import Roles
from core.utils.lite_dependencies import role_require

@router.post("/private/edit")
async def edit_data(
    _: Annotated[None, Depends(role_require(Roles.methodist))]
):
    return {"success": True}
```

### Скрытие email в логах
```python
from core.utils.anything import hide_log_param
from core.utils.logger import log_event

email = "user@example.com"
log_event(f"Попытка входа: {hide_log_param(email)}")
# Лог: "Попытка входа: use***@exa***.com"
```

### Извлечение IP адреса
```python
from core.utils.anything import get_client_ip

@router.post("/login")
async def login(request: Request):
    client_ip = get_client_ip(request)
    log_event(f"Вход с IP: {client_ip}")
```

### Парсинг ошибки конфликта
```python
from core.utils.anything import extract_conflict_values

try:
    await db.groups.add_group(name="23И1")
except Exception as e:
    if "duplicate key" in str(e):
        conflicts = extract_conflict_values(str(e))
        log_event(f"Конфликт: {conflicts}")
        # Конфликт: [('group_name', '=23И1')]
```

## Типичные сценарии

1. **Проверка роли пользователя:**
   ```python
   if request.state.role == Roles.methodist:
       # Показать кнопку "Редактировать"
   ```

2. **Фильтрация версий по статусу:**
   ```python
   pending_versions = await db.ttable.get_all(
       status_id=TimetableVerStatuses.pending
   )
   ```

3. **Логирование с скрытием данных:**
   ```python
   log_event(
       f"Регистрация: {hide_log_param(email)}",
       level='INFO'
   )
   ```

4. **Извлечение реального IP:**
   ```python
   client_ip = get_client_ip(request)
   await redis.incr(f"rate_limit:{client_ip}")
   ```

## Безопасность

### Скрытие чувствительных данных
- `hide_log_param()` предотвращает утечку email/токенов через логи
- Важно использовать для всех PII (Personally Identifiable Information)

### Защита от IP spoofing
- `get_client_ip()` доверяет только `trusted_proxies`
- Предотвращает подделку IP через заголовки
- Критично для rate limiting и безопасности

### Константы вместо magic strings
- Использование `Roles.methodist` вместо `"methodist"`
- Предотвращает опечатки
- Упрощает рефакторинг

## Ограничения

1. **Только две роли:**
   - Недостаточно для сложных систем
   - Нет гранулярного контроля доступа

2. **Статусы жёстко закодированы:**
   - Нельзя добавить новые статусы без изменения кода
   - Нет динамической конфигурации

3. **hide_log_param() для коротких строк:**
   - Может выглядеть странно для строк < 11 символов
   - Нет проверки минимальной длины

4. **extract_conflict_values() хрупкий:**
   - Зависит от формата ошибок PostgreSQL
   - Может сломаться при изменении формата

## Будущие улучшения

### 1. Расширение ролей
```python
@dataclass
class Roles:
    admin: str = 'admin'
    methodist: str = 'methodist'
    teacher: str = 'teacher'
    student: str = 'student'
    read_all: str = 'read_all'
```

### 2. Динамические статусы
```python
# Загрузка из БД или конфига
statuses = await db.statuses.get_all()
TimetableVerStatuses = create_status_class(statuses)
```

### 3. Улучшение hide_log_param()
```python
def hide_log_param(param: str, start: int = 3, end: int = 8) -> str:
    if len(param) < start + end + 3:
        # Слишком короткая строка
        return param[:2] + '***' + param[-2:]
    return param[:start] + '*' * len(param[start:-end-1]) + param[-end:]
```

### 4. Типизация get_client_ip()
```python
from typing import Union
from ipaddress import IPv4Address, IPv6Address

def get_client_ip(request: Request | WebSocket) -> Union[IPv4Address, IPv6Address]:
    # Возвращать IP объект вместо строки
    # Позволяет проверять диапазоны, подсети и т.д.
```

### 5. Enum вместо dataclass
```python
from enum import Enum

class Roles(str, Enum):
    METHODIST = 'methodist'
    READ_ALL = 'read_all'

# Преимущества:
# - Автодополнение в IDE
# - Валидация значений
# - Итерация по всем ролям
```

## Связь с документацией

Модуль используется в:
- `docs/api/*` - все API модули используют статусы и роли
- `docs/utils/lite_dependencies.md` - использует `Roles`
- `docs/utils/logger.md` - использует `hide_log_param()`
- `docs/api/middleware.md` - использует `get_client_ip()`
- Практически вся документация ссылается на этот модуль
