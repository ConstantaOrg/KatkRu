# 📄 Schemas to Dependencies

## Описание
Утилитный модуль с переиспользуемыми Pydantic схемами для пагинации. Схемы используются как FastAPI dependencies через `Depends()` для автоматической валидации параметров запросов.

## Схемы

### 📋 `PagenSchema`
Базовая схема пагинации для большинства эндпоинтов.

**Поля:**
```python
class PagenSchema(BaseModel):
    offset: int = Field(default=0, ge=0)
    limit: int = Field(default=10, ge=1)
```

- `offset` (int, default=0, >=0) - смещение для пагинации (с какой записи начинать)
- `limit` (int, default=10, >=1) - количество записей на странице

**Использование:**
```python
@router.post("/get_all")
async def get_all(pagen: PagenSchema):
    results = await db.query(limit=pagen.limit, offset=pagen.offset)
    return {"results": results}
```

---

### 📦 `PagenDep`
Готовая dependency для использования в эндпоинтах.

**Определение:**
```python
PagenDep = Annotated[PagenSchema, Depends(PagenSchema)]
```

**Использование:**
```python
@router.post("/get_all")
async def get_all(pagen: PagenDep):
    # pagen автоматически валидируется и инжектится
    results = await db.query(limit=pagen.limit, offset=pagen.offset)
    return {"results": results}
```

**Преимущества:**
- Автоматическая валидация параметров
- Переиспользуемый код
- Типизация для IDE

---

### 👥 `GroupPagenSchema`
Схема пагинации для групп с увеличенным лимитом по умолчанию.

**Поля:**
```python
class GroupPagenSchema(PagenSchema):
    limit: int = Field(default=30, ge=1)
```

- Наследует `offset` от `PagenSchema`
- Переопределяет `limit` с default=30

**Причина:**
- Групп обычно больше, чем других сущностей
- Удобнее отображать больше записей на странице

---

### 👨‍🏫 `TeachersPagenSchema`
Схема пагинации для преподавателей с увеличенным лимитом.

**Поля:**
```python
class TeachersPagenSchema(PagenSchema):
    limit: int = Field(default=40, ge=1)
```

- Наследует `offset` от `PagenSchema`
- Переопределяет `limit` с default=40

**Причина:**
- Преподавателей может быть много
- Максимальный лимит среди всех схем пагинации

---

### 📖 `DisciplinesPagenSchema`
Схема пагинации для дисциплин (алиас для `GroupPagenSchema`).

**Определение:**
```python
class DisciplinesPagenSchema(GroupPagenSchema):
    ...
```

- Полностью идентична `GroupPagenSchema`
- Используется для семантической ясности кода
- `limit` default=30

## Бизнес-логика

### Пагинация

**Стандартная пагинация:**
- По умолчанию: 10 записей на странице
- Используется для большинства эндпоинтов

**Увеличенная пагинация:**
- Группы и дисциплины: 30 записей
- Преподаватели: 40 записей
- Обусловлено количеством сущностей в системе

### Валидация

**Ограничения:**
- `offset >= 0` - не может быть отрицательным
- `limit >= 1` - минимум 1 запись на странице
- Нет максимального лимита (можно добавить при необходимости)

**Автоматическая валидация:**
```python
# Запрос с невалидными параметрами
POST /api/v1/groups/get_all
{
  "offset": -5,  # Ошибка: должно быть >= 0
  "limit": 0     # Ошибка: должно быть >= 1
}

# FastAPI автоматически вернёт 422 Unprocessable Entity
```

## Зависимости

### Внешние библиотеки
- `pydantic.BaseModel` - базовый класс для схем
- `pydantic.Field` - определение полей с валидацией
- `fastapi.Depends` - механизм dependency injection
- `typing.Annotated` - аннотации типов

### Использование в модулях

Схемы используются в:
- `core/api/groups_tab.py` - `GroupPagenSchema`
- `core/api/teachers_tab.py` - `TeachersPagenSchema`
- `core/api/disciplines_tab.py` - `DisciplinesPagenSchema`
- `core/api/timetable_api.py` - `PagenSchema`
- `core/api/elastic_search/api_elastic_search.py` - `PagenSchema`
- `core/api/ttable_versions/ttable_versions_tab.py` - `PagenSchema`
- И другие модули

## Примеры использования

### Базовая пагинация
```python
from core.schemas.schemas2depends import PagenSchema

@router.post("/users/get_all")
async def get_users(pagen: PagenSchema, db: PgSqlDep):
    users = await db.users.get_all(
        limit=pagen.limit,
        offset=pagen.offset
    )
    return {"users": users}

# Запрос
POST /api/v1/users/get_all
{
  "offset": 0,
  "limit": 10
}

# Или с дефолтными значениями
POST /api/v1/users/get_all
{}  # offset=0, limit=10
```

### Пагинация с dependency
```python
from core.schemas.schemas2depends import PagenDep

@router.post("/users/get_all")
async def get_users(pagen: PagenDep, db: PgSqlDep):
    # Идентично предыдущему примеру
    users = await db.users.get_all(
        limit=pagen.limit,
        offset=pagen.offset
    )
    return {"users": users}
```

### Специализированная пагинация
```python
from core.schemas.schemas2depends import GroupPagenSchema

@router.post("/groups/get_all")
async def get_groups(pagen: GroupPagenSchema, db: PgSqlDep):
    groups = await db.groups.get_all(
        limit=pagen.limit,  # default=30
        offset=pagen.offset
    )
    return {"groups": groups}

# Запрос с дефолтными значениями
POST /api/v1/groups/get_all
{}  # offset=0, limit=30
```

### Вычисление страниц
```python
# Получение второй страницы (записи 10-19)
{
  "offset": 10,
  "limit": 10
}

# Получение третьей страницы (записи 20-29)
{
  "offset": 20,
  "limit": 10
}

# Формула: offset = page_number * limit
```

## Типичные сценарии

1. **Первая страница:**
   - Пользователь открывает список
   - Фронтенд отправляет `{"offset": 0, "limit": 10}`
   - Получает первые 10 записей

2. **Следующая страница:**
   - Пользователь кликает "Далее"
   - Фронтенд увеличивает offset: `{"offset": 10, "limit": 10}`
   - Получает записи 10-19

3. **Изменение размера страницы:**
   - Пользователь выбирает "Показать 50 записей"
   - Фронтенд отправляет `{"offset": 0, "limit": 50}`
   - Получает первые 50 записей

4. **Прямой переход на страницу:**
   - Пользователь вводит номер страницы: 5
   - Фронтенд вычисляет: `offset = (5-1) * 10 = 40`
   - Отправляет `{"offset": 40, "limit": 10}`

## Преимущества подхода

### Переиспользование кода
- Одна схема используется во многих эндпоинтах
- Изменения в одном месте применяются везде
- Меньше дублирования кода

### Автоматическая валидация
- FastAPI валидирует параметры автоматически
- Не нужно писать проверки вручную
- Генерируется OpenAPI документация

### Типизация
- IDE подсказывает поля схемы
- Автодополнение работает корректно
- Меньше ошибок при разработке

### Семантическая ясность
- `GroupPagenSchema` явно указывает на пагинацию групп
- Код становится более читаемым
- Легче понять назначение параметров

## Будущие улучшения

1. **Максимальный лимит:**
   - Добавить `le=100` для предотвращения перегрузки
   - Защита от запросов с огромным limit

2. **Cursor-based пагинация:**
   - Для больших датасетов
   - Более эффективна, чем offset-based

3. **Метаданные пагинации:**
   - Добавить `total_count` в ответ
   - Информация о количестве страниц
   - Текущая страница

4. **Сортировка:**
   - Добавить поля `sort_by` и `sort_order`
   - Интеграция с пагинацией

## Связь с документацией

Схемы пагинации используются в:
- `docs/api/groups_tab.md` - пагинация групп
- `docs/api/teachers_tab.md` - пагинация преподавателей
- `docs/api/disciplines_tab.md` - пагинация дисциплин
- `docs/api/timetable_api.md` - пагинация расписания
- `docs/api/elastic_search_api_elastic_search.md` - пагинация результатов поиска
- `docs/api/ttable_versions_ttable_versions_tab.md` - пагинация версий расписания
