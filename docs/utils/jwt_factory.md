# Утилита: jwt_factory

## Описание

Модуль `jwt_factory.py` отвечает за полный жизненный цикл JWT токенов в системе аутентификации: создание, валидацию и обновление токенов. Модуль поддерживает три типа токенов (access, refresh, websocket), управление мульти-сессиями пользователей на разных устройствах и обеспечивает безопасное хранение refresh токенов в базе данных.

**Бизнес-контекст:** Система используется для аутентификации методистов и других пользователей, связанных с составлением расписания в учебном заведении. Методисты привязаны к конкретному зданию (корпусу) и имеют доступ только к расписаниям своего здания.

## Типы токенов

Модуль работает с тремя типами JWT токенов:

- **Access Token (aT)** - короткоживущий токен для доступа к API (TTL: 15 минут)
- **Refresh Token (rT)** - долгоживущий токен для обновления access токена (TTL: 30 дней)
- **WebSocket Token (wT)** - токен для WebSocket соединений (TTL: 60 секунд, в разработке)

## Структура payload токена

Все токены содержат следующие поля:

- `sub` (subject) - ID пользователя
- `role` - роль пользователя (`methodist` или `read_all`)
- `bid` (building_id) - ID здания/корпуса учебного заведения
- `s_id` (session_id) - уникальный идентификатор сессии (только для access токена)
- `iat` (issued at) - время создания токена
- `exp` (expiration) - время истечения токена

**Роли:**
- `methodist` - методист, имеет доступ к расписаниям своего здания
- `read_all` - разработчик, имеет доступ ко всем данным

## Функции

### 🔐 set_jwt_encode(payload: dict[str, Any])

**Сигнатура:** `def set_jwt_encode(payload: dict[str, Any]) -> str`

**Описание:** Кодирует payload в JWT токен с использованием приватного ключа и алгоритма RS256.

**Параметры:**
- `payload` (dict[str, Any]): словарь с данными для кодирования в токен

**Возвращает:**
- `str`: закодированный JWT токен

**Бизнес-логика:**
Функция использует приватный ключ из конфигурации (`env.JWTs.private_key`) и алгоритм RS256 для создания подписанного JWT токена. Это базовая функция кодирования, используемая всеми остальными функциями модуля.

**Примеры использования:**
```python
from core.utils.jwt_factory import set_jwt_encode

payload = {
    'sub': '123',
    'role': 'methodist',
    'bid': '1',
    'iat': datetime.now(UTC),
    'exp': datetime.now(UTC) + timedelta(minutes=15)
}
token = set_jwt_encode(payload)
```

---

### 🔓 get_jwt_decode_payload(encoded_jwt: str, public_key: str | None = None, verify_exp: bool = False, audience: str | None = None)

**Сигнатура:** `def get_jwt_decode_payload(encoded_jwt: str, public_key: str | None=None, verify_exp: bool=False, audience: str | None=None) -> dict | int`

**Описание:** Декодирует JWT токен и возвращает payload. В случае ошибки возвращает код 401.

**Параметры:**
- `encoded_jwt` (str): закодированный JWT токен
- `public_key` (str | None): публичный ключ для проверки подписи (по умолчанию используется ключ из конфигурации)
- `verify_exp` (bool): проверять ли истечение токена (по умолчанию False)
- `audience` (str | None): ожидаемая аудитория токена

**Возвращает:**
- `dict`: декодированный payload токена
- `int`: код 401 в случае ошибки декодирования или истечения токена

**Бизнес-логика:**
Функция декодирует JWT токен с использованием публичного ключа и алгоритма RS256. Используется `leeway=10` (10 секунд) для компенсации рассинхронизации времени между серверами. При ошибках декодирования (`DecodeError`) или истечения токена (`ExpiredSignatureError`) возвращается код 401, который обрабатывается на уровне middleware.

**Обработка ошибок:**
- `DecodeError`: невалидный токен или неверная подпись → возврат 401
- `ExpiredSignatureError`: токен истёк → возврат 401

**Примеры использования:**
```python
from core.utils.jwt_factory import get_jwt_decode_payload

# Декодирование без проверки истечения
payload = get_jwt_decode_payload(token)
if payload == 401:
    # Обработка ошибки
    pass

# Декодирование с проверкой истечения
payload = get_jwt_decode_payload(token, verify_exp=True)
```

---

### ⏱️ add_ttl_limit(data: dict, token_ttl: str)

**Сигнатура:** `def add_ttl_limit(data: dict, token_ttl: str) -> dict`

**Описание:** Добавляет в payload поля `iat` (issued at) и `exp` (expiration) в зависимости от типа токена.

**Параметры:**
- `data` (dict): payload токена
- `token_ttl` (str): тип токена ('access_token', 'refresh_token' или 'ws_token')

**Возвращает:**
- `dict`: payload с добавленными полями `iat` и `exp`

**Бизнес-логика:**
Функция определяет TTL (time to live) токена в зависимости от его типа:
- Access Token: 15 минут
- Refresh Token: 30 дней
- WebSocket Token: 60 секунд

Время создания (`iat`) устанавливается в текущее время UTC, время истечения (`exp`) рассчитывается как `iat + TTL`.

**Примеры использования:**
```python
from core.utils.jwt_factory import add_ttl_limit

payload = {'sub': '123', 'role': 'methodist', 'bid': '1'}
payload_with_ttl = add_ttl_limit(payload, 'access_token')
# payload_with_ttl теперь содержит iat и exp
```

---

### 🎫 issue_token(payload: dict, token: TokenTypes | str, db: PgSqlDep = None, session_id: str | None = None, client: TokenPayloadSchema = None)

**Сигнатура:** `async def issue_token(payload: dict, token: TokenTypes | str, db: PgSqlDep = None, session_id: str | None=None, client: TokenPayloadSchema=None) -> str`

**Описание:** Создаёт токен указанного типа. Для refresh токена дополнительно сохраняет хешированную версию в базе данных.

**Параметры:**
- `payload` (dict): данные для токена (sub, role, bid)
- `token` (TokenTypes | str): тип токена ('access_token', 'refresh_token' или 'ws_token')
- `db` (PgSqlDep): подключение к базе данных (обязательно для refresh токена)
- `session_id` (str | None): ID сессии (обязательно для access и refresh токенов)
- `client` (TokenPayloadSchema): информация о клиенте (user_agent, ip) - обязательно для refresh токена

**Возвращает:**
- `str`: для access и ws токенов - закодированный JWT токен
- `str`: для refresh токена - хешированная версия токена (сохранённая в БД)

**Бизнес-логика:**

**Для Refresh Token:**
1. Добавляет TTL (30 дней) к payload
2. Кодирует токен с помощью `set_jwt_encode`
3. Хеширует закодированный токен с помощью `encryption.hash` (Argon2)
4. Сохраняет хешированный токен в БД вместе с метаданными сессии (session_id, user_id, iat, exp, user_agent, ip)
5. Возвращает хешированную версию

**Причина хеширования:** Refresh токен до хеширования содержит уникальные поля `exp`, `iat` и `user_id`, что делает каждый хеш гарантированно уникальным. Это полностью скрывает `session_id` и предотвращает установление связи между новыми и старыми refresh токенами. Даже при компрометации БД атакующий не сможет определить, какие токены принадлежат одной сессии.

**Для Access Token:**
1. Добавляет `s_id` (session_id) к payload
2. Добавляет TTL (15 минут)
3. Кодирует и возвращает токен

**Для WebSocket Token:**
1. Добавляет TTL (60 секунд)
2. Кодирует и возвращает токен

**Примеры использования:**
```python
from core.utils.jwt_factory import issue_token
from core.schemas.users_schema import TokenPayloadSchema

# Создание access токена
payload = {'sub': '123', 'role': 'methodist', 'bid': '1'}
access_token = await issue_token(payload, 'access_token', session_id='uuid-here')

# Создание refresh токена
client_info = TokenPayloadSchema(
    id=123,
    role='methodist',
    building_id=1,
    user_agent='Mozilla/5.0...',
    ip='192.168.1.1'
)
hashed_refresh = await issue_token(
    payload, 
    'refresh_token', 
    db=db, 
    session_id='uuid-here',
    client=client_info
)
```

---

### 🚀 issue_aT_rT(db: PgSqlDep, token_schema: TokenPayloadSchema)

**Сигнатура:** `async def issue_aT_rT(db: PgSqlDep, token_schema: TokenPayloadSchema) -> tuple[str, str]`

**Описание:** Выдаёт пару токенов (access и refresh) при входе пользователя в систему. Поддерживает мульти-сессии на разных устройствах.

**Параметры:**
- `db` (PgSqlDep): подключение к базе данных
- `token_schema` (TokenPayloadSchema): схема с данными пользователя (id, role, building_id, user_agent, ip)

**Возвращает:**
- `tuple[str, str]`: кортеж (access_token, hashed_refresh_token)

**Бизнес-логика:**

1. **Проверка существующей сессии:** Проверяет, есть ли активная сессия для данного пользователя и user_agent
   - Если сессия найдена → переиспользует существующий session_id
   - Если сессии нет → генерирует новый UUID для session_id

2. **Мульти-сессии:** Система поддерживает несколько активных сессий для одного пользователя на разных устройствах. Идентификация устройства происходит по `user_agent`.

3. **Создание токенов:**
   - Формирует базовый payload с полями: sub (user_id), role, bid (building_id)
   - Создаёт refresh токен (хешируется и сохраняется в БД)
   - Создаёт access токен с добавлением session_id

4. **Логирование:** Логирует выдачу токенов с уровнем WARNING для мониторинга

**Примеры использования:**
```python
from core.utils.jwt_factory import issue_aT_rT
from core.schemas.users_schema import TokenPayloadSchema

token_schema = TokenPayloadSchema(
    id=123,
    role='methodist',
    building_id=1,
    user_agent='Mozilla/5.0 (Windows NT 10.0; Win64; x64)...',
    ip='192.168.1.100'
)

access_token, hashed_refresh = await issue_aT_rT(db, token_schema)
# Возвращаем access_token клиенту, hashed_refresh сохранён в БД
```

---

### 🔄 reissue_aT(access_token: dict, refresh_token: str, db: PgSqlDep)

**Сигнатура:** `async def reissue_aT(access_token: dict, refresh_token: str, db: PgSqlDep) -> str | int`

**Описание:** Обновляет истёкший access токен с использованием refresh токена. Проверяет валидность refresh токена через сравнение с хешем в БД.

**Параметры:**
- `access_token` (dict): декодированный payload истёкшего access токена
- `refresh_token` (str): хешированный refresh токен от клиента
- `db` (PgSqlDep): подключение к базе данных

**Возвращает:**
- `str`: новый access токен при успешной валидации
- `int`: код 401 при ошибке валидации

**Бизнес-логика:**

1. **Извлечение данных:** Извлекает из истёкшего access токена: sub (user_id), s_id (session_id), role, bid

2. **Валидация полей:** Проверяет наличие обязательных полей (sub, s_id). Если отсутствуют → возврат 401

3. **Получение refresh токена из БД:** Запрашивает актуальный refresh токен для данного пользователя и сессии
   - SQL запрос проверяет, что токен не истёк (`exp > now()`)

4. **Сравнение токенов:** Использует `secrets.compare_digest()` для безопасного сравнения refresh токенов
   - **Защита от timing attacks:** Функция `compare_digest` выполняет сравнение за константное время, предотвращая атаки по времени выполнения

5. **Результаты валидации:**
   - **Успех:** Токены совпали и refresh токен не истёк → создаётся новый access токен
   - **Ошибка:** Токен истёк, не совпал или отозван → возврат 401

6. **Логирование:**
   - При успехе: логируется выпуск нового access токена
   - При ошибке: логируется событие с уровнем CRITICAL (индикатор возможной атаки или компрометации)

**Обработка ошибок:**

- Отсутствие `sub` или `s_id` в access токене → 401
- Refresh токен не найден в БД (истёк или отозван) → 401 + CRITICAL лог
- Refresh токен не совпал с БД (возможная атака) → 401 + CRITICAL лог

**Примечание:** Access токен короткоживущий (15 минут), поэтому отзыв всех сессий при несовпадении refresh токена не критичен. Пользователю потребуется повторная аутентификация.

**Примеры использования:**
```python
from core.utils.jwt_factory import reissue_aT, get_jwt_decode_payload

# Декодируем истёкший access токен (без проверки exp)
expired_access = get_jwt_decode_payload(old_access_token, verify_exp=False)

# Пытаемся обновить access токен
new_access_token = await reissue_aT(expired_access, hashed_refresh_token, db)

if new_access_token == 401:
    # Требуется повторная аутентификация
    return {"error": "Unauthorized"}
else:
    # Возвращаем новый токен клиенту
    return {"access_token": new_access_token}
```

## Зависимости

- `jwt` - библиотека для работы с JWT токенами
- `secrets` - модуль для безопасного сравнения строк (защита от timing attacks)
- `datetime` - работа с временными метками
- `uuid` - генерация уникальных идентификаторов сессий
- `core.config_dir.config` - конфигурация (ключи, алгоритм, TTL)
- `core.data.postgre` - подключение к PostgreSQL для управления сессиями
- `core.schemas.users_schema` - схемы валидации данных пользователя
- `core.utils.anything` - константы типов токенов
- `core.utils.logger` - логирование событий

## Обработка ошибок

### Возврат кода 401

Модуль использует подход возврата числа `401` вместо выбрасывания исключений. Это упрощает обработку ошибок на уровне middleware и позволяет единообразно обрабатывать все случаи неавторизованного доступа.

**Случаи возврата 401:**
- Невалидный JWT токен (DecodeError)
- Истёкший JWT токен (ExpiredSignatureError)
- Отсутствие обязательных полей в payload (sub, s_id)
- Несовпадение refresh токена с БД
- Истечение refresh токена

### Логирование критических событий

При несовпадении refresh токена логируется событие с уровнем `CRITICAL`. Это индикатор возможной атаки или компрометации токена. Критические логи используются для мониторинга безопасности системы.

**Пример критического лога:**
```
CRITICAL: refresh_token истёк или подменён | s_id: uuid-here; user_id: 123
```

## Безопасность

### Хеширование refresh токенов

Refresh токены хешируются с использованием Argon2 перед сохранением в базе данных. Каждый refresh токен до хеширования содержит уникальные временные метки (`iat`, `exp`) и `user_id`, что делает каждый хеш гарантированно уникальным.

**Преимущества:**
- Защита от компрометации токенов при утечке данных из БД
- Полное сокрытие `session_id` - атакующий не может извлечь его из хеша
- Невозможность установить связь между новыми и старыми refresh токенами одной сессии
- Каждый refresh токен имеет уникальный хеш благодаря уникальным временным меткам

### Защита от timing attacks

Сравнение refresh токенов выполняется с помощью `secrets.compare_digest()`, который работает за константное время. Это предотвращает атаки, основанные на измерении времени выполнения операции сравнения.

### Leeway для синхронизации времени

При декодировании токенов используется `leeway=10` (10 секунд) для компенсации рассинхронизации времени между серверами. Это предотвращает ложные отказы из-за небольших расхождений в системном времени.

### Мульти-сессии

Система поддерживает несколько активных сессий для одного пользователя на разных устройствах. Каждая сессия идентифицируется по комбинации `user_id` + `user_agent` + `session_id`.

## Управление сессиями

### Хранение сессий

Информация о сессиях хранится в таблице `sessions_users` в PostgreSQL:
- `session_id` - уникальный идентификатор сессии (UUID)
- `user_id` - ID пользователя
- `iat` - время создания сессии
- `exp` - время истечения сессии
- `user_agent` - User-Agent браузера/приложения
- `ip` - IP адрес клиента
- `refresh_token` - хешированный refresh токен

### Очистка старых сессий

Записи о сессиях хранятся в БД в течение 6 месяцев после истечения refresh токена. Очистка выполняется с помощью cron-задачи, которая вызывает функцию `slam_refresh_tokens` из модуля `core/data/sql_queries/users_sql.py`.

## Примечания

- **WebSocket токены** находятся в разработке и пока не используются активно в системе
- **Алгоритм подписи:** RS256 (RSA с SHA-256)
- **Ключи:** Приватный и публичный ключи загружаются из файлов или Docker secrets
- **Часовой пояс:** Все временные метки используют UTC
- **Мониторинг:** Выдача токенов логируется с уровнем WARNING, критические события (несовпадение refresh токенов) - с уровнем CRITICAL
