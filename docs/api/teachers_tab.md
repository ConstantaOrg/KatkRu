# Модуль: teachers_tab

## Описание

Модуль `teachers_tab.py` предоставляет API для управления преподавателями в системе расписания. Включает CRUD операции и управление статусами (активные/устаревшие).

### Ключевые возможности

- 👨‍🏫 Просмотр списка преподавателей с фильтрацией и пагинацией
- ➕ Добавление новых преподавателей
- 🔄 Обновление статусов (активные ↔ устаревшие)
- 🔐 Защита через JWT и проверку ролей
- 📝 Хранение ФИО преподавателей

### Концепция статусов

Преподаватели имеют два статуса:
- **Активные** (`is_active = true`) - работающие преподаватели
- **Устаревшие** (`is_active = false`) - уволившиеся или ушедшие на пенсию

Это позволяет:
- Скрывать неактивных преподавателей из интерфейса
- Сохранять историю расписания
- Восстанавливать преподавателей при необходимости

## Эндпоинты

### 📖 POST /api/v1/private/teachers/get

**Описание:** Получение списка преподавателей с фильтрацией по статусу и пагинацией. Используется для отображения преподавателей в интерфейсе методиста.

**Middleware:**
- `role_require(Roles.methodist, Roles.read_all)` - Требуется роль методиста или read_all
- `JWTCookieDep` - JWT аутентификация через cookie

**Параметры запроса (query):**
- `limit` (int): Количество записей на странице (по умолчанию из TeachersPagenSchema)
- `offset` (int): Смещение для пагинации (по умолчанию 0)

**Тело запроса:**
```json
{
  "is_active": true
}
```

Поля:
- `is_active` (bool | None): Фильтр по статусу. `true` - активные, `false` - устаревшие, `null` - все

**Ответ (200):**
```json
{
  "teachers": [
    {
      "id": 1,
      "fio": "Иванов Иван Иванович",
      "is_active": true
    },
    {
      "id": 2,
      "fio": "Петрова Анна Сергеевна",
      "is_active": true
    },
    {
      "id": 3,
      "fio": "Сидоров Пётр Константинович",
      "is_active": true
    }
  ]
}
```

Поля преподавателя:
- `id` (int): ID преподавателя
- `fio` (str): ФИО преподавателя (Фамилия Имя Отчество)
- `is_active` (bool): Статус (активный/устаревший)

**Используемые функции:**
- `db.teachers.get_all()` - Получение преподавателей с фильтрацией и пагинацией
- `log_event()` - Логирование запроса

**Бизнес-логика:**

1. Извлекаются параметры:
   - `is_active` - из тела запроса
   - `limit`, `offset` - из query параметров

2. Запрос к БД с фильтрами:
   - Если `is_active` указан - фильтрует по статусу
   - Применяется пагинация (limit/offset)

3. Логирование с user_id (красный цвет)

4. Возврат списка преподавателей

**Примечания:**
- Пагинация для эффективной работы с большими списками
- Роль read_all позволяет просматривать без изменения
- Преподаватели не привязаны к корпусам (общие для всей системы)

---

### 🔄 PUT /api/v1/private/teachers/update

**Описание:** Массовое обновление статусов преподавателей. Позволяет активировать устаревших преподавателей или деактивировать активных. Возвращает количество обновлённых записей.

**Middleware:**
- `role_require(Roles.methodist)` - Требуется роль методиста
- `JWTCookieDep` - JWT аутентификация через cookie

**Параметры запроса:** Нет

**Тело запроса:**
```json
{
  "set_as_active": [1, 2, 3],
  "set_as_deprecated": [4, 5]
}
```

Поля:
- `set_as_active` (list[int]): Список ID преподавателей для активации
- `set_as_deprecated` (list[int]): Список ID преподавателей для деактивации

**Ответ (200):**
```json
{
  "success": true,
  "message": "Учителя сменили статусы",
  "active_upd_count": 3,
  "depr_upd_count": 2
}
```

Поля ответа:
- `success` (bool): Успешность операции
- `message` (str): Сообщение о результате
- `active_upd_count` (int): Количество активированных преподавателей
- `depr_upd_count` (int): Количество деактивированных преподавателей

**Используемые функции:**
- `db.teachers.switch_status()` - Массовое обновление статусов
- `log_event()` - Логирование операции

**Бизнес-логика:**

1. Получение списков ID для обновления:
   - `set_as_active` - преподаватели для активации
   - `set_as_deprecated` - преподаватели для деактивации

2. Выполнение обновления в БД:
   - Устанавливает `is_active = true` для преподавателей из `set_as_active`
   - Устанавливает `is_active = false` для преподавателей из `set_as_deprecated`
   - Возвращает количество обновлённых записей

3. Логирование с полным дампом входных данных

4. Возврат результата с счётчиками

**Примечания:**
- Массовая операция эффективнее множественных одиночных запросов
- Счётчики позволяют проверить, все ли преподаватели обновлены
- Можно активировать и деактивировать за один запрос

---

### ➕ POST /api/v1/private/teachers/add

**Описание:** Добавление нового преподавателя в систему. Проверяет уникальность ФИО и возвращает ID созданного преподавателя.

**Middleware:**
- `role_require(Roles.methodist)` - Требуется роль методиста
- `JWTCookieDep` - JWT аутентификация через cookie

**Параметры запроса:** Нет

**Тело запроса:**
```json
{
  "fio": "Смирнов Алексей Владимирович"
}
```

Поля:
- `fio` (str): ФИО преподавателя (Фамилия Имя Отчество, уникальное)

**Ответ (200) - Успешное добавление:**
```json
{
  "success": true,
  "message": "Учитель добавлен",
  "teacher_id": 42
}
```

**Ответ (409) - Преподаватель уже существует:**
```json
{
  "success": false,
  "message": "Такой учитель уже добавлен",
  "detail": "Такой учитель уже добавлен"
}
```

**Используемые функции:**
- `db.teachers.add()` - Добавление преподавателя в БД
- `create_teachers_add_response()` - Создание типизированного ответа
- `create_response_json()` - Создание JSON ответа с статус-кодом
- `log_event()` - Логирование операции

**Бизнес-логика:**

1. Извлечение параметров:
   - `fio` - из тела запроса

2. Попытка добавления преподавателя в БД:
   - Проверяется уникальность ФИО
   - Если ФИО уже существует - возвращается `None`

3. Обработка результата:
   - **Если `teacher_id` is None:**
     - Логируется WARNING с ФИО
     - Возвращается 409 Conflict
   - **Если успешно:**
     - Логируется событие с ID и ФИО
     - Возвращается 200 OK с ID преподавателя

4. Цветовое кодирование в логах:
   - user_id - красный
   - fio, teacher_id - синий

**Примечания:**
- Новый преподаватель создаётся со статусом `is_active = true`
- ФИО должно быть уникальным в системе
- Формат ФИО: "Фамилия Имя Отчество"
- Преподаватели общие для всех корпусов

---

## Функции

Модуль использует только эндпоинты (route handlers), отдельных вспомогательных функций не содержит. Вся бизнес-логика реализована через вызовы методов слоя данных (`db.*`) и утилит.

## Зависимости

### 🌐 Внешние библиотеки
- `fastapi` - Фреймворк для создания API
  - `APIRouter` - Роутер для группировки эндпоинтов
  - `Depends` - Система зависимостей
- `starlette.requests.Request` - Объект HTTP запроса

### 🔗 Внутренние модули

**Слой данных:**
- `core.data.postgre.PgSqlDep` - Зависимость для доступа к PostgreSQL
  - `db.teachers.get_all()` - Получение списка преподавателей
  - `db.teachers.switch_status()` - Обновление статусов
  - `db.teachers.add()` - Добавление преподавателя

**Response схемы:**
- `core.response_schemas.teachers_tab` - Схемы ответов:
  - `TeachersGetResponse` - Список преподавателей
  - `TeachersUpdateResponse` - Результат обновления статусов
- `core.utils.response_model_utils` - Утилиты для создания ответов:
  - `TeachersAddSuccessResponse` - Успешное добавление
  - `TeachersAddConflictResponse` - Конфликт при добавлении
  - `create_teachers_add_response()` - Фабрика ответов
  - `create_response_json()` - Создание JSON ответа

**Request схемы:**
- `core.schemas.n8n_ui.teachers_schema` - Схемы запросов:
  - `TeacherFilterSchema` - Фильтрация (is_active)
  - `TeachersUpdateSchema` - Обновление статусов (set_as_active, set_as_deprecated)
  - `TeachersAddSchema` - Добавление (fio)
- `core.schemas.schemas2depends.TeachersPagenSchema` - Пагинация (limit, offset)
- `core.schemas.cookie_settings_schema.JWTCookieDep` - JWT аутентификация

**Утилиты:**
- `core.utils.anything.Roles` - Роли пользователей (methodist, read_all)
- `core.utils.lite_dependencies.role_require` - Проверка ролей
- `core.utils.logger.log_event` - Логирование событий

## ⚠️ Обработка ошибок

### 409 Conflict - Преподаватель уже существует

**Эндпоинт:** `POST /api/v1/private/teachers/add`

**Причина:** Преподаватель с таким ФИО уже существует в системе

**Ответ:**
```json
{
  "success": false,
  "message": "Такой учитель уже добавлен",
  "detail": "Такой учитель уже добавлен"
}
```

**Логирование:** WARNING - "Не удалось добавить учителя"

**Примечание:** Уникальность проверяется по полному ФИО. Если есть однофамильцы, нужно добавлять отличительные признаки (например, инициалы отчества).

## 💡 Примечания

### Отсутствие привязки к корпусам

В отличие от групп, преподаватели НЕ привязаны к корпусам:
- Преподаватели общие для всей системы
- Один преподаватель может вести занятия в разных корпусах
- Методисты всех корпусов видят одних и тех же преподавателей

### Статусы преподавателей

Система использует мягкое удаление (soft delete):
- Преподаватели не удаляются физически
- Вместо этого меняется статус `is_active`
- Это сохраняет историю расписания
- Устаревших преподавателей можно восстановить

### Уникальность ФИО

Уникальность проверяется на уровне БД:
- Constraint: `UNIQUE (fio)`
- ФИО должно быть уникальным в системе
- Формат: "Фамилия Имя Отчество"

### Пагинация

Параметры пагинации передаются через query:
- `limit` - количество записей на странице
- `offset` - смещение (номер страницы * limit)
- Позволяет эффективно работать с большими списками

### Цветовое кодирование в логах

Логи используют ANSI escape codes:
- `\033[31m` - красный (user_id)
- `\033[34m` - синий (fio, teacher_id)
- `\033[0m` - сброс цвета

## 💻 Примеры использования

### 📖 Получение всех активных преподавателей

```python
POST /api/v1/private/teachers/get?limit=20&offset=0
Content-Type: application/json
Cookie: access_token=...; refresh_token=...

{
  "is_active": true
}

# Ответ (200):
{
  "teachers": [
    {"id": 1, "fio": "Иванов Иван Иванович", "is_active": true},
    {"id": 2, "fio": "Петрова Анна Сергеевна", "is_active": true},
    {"id": 3, "fio": "Сидоров Пётр Константинович", "is_active": true}
  ]
}
```

### 📚 Получение всех преподавателей (активные + устаревшие)

```python
POST /api/v1/private/teachers/get?limit=50&offset=0
Content-Type: application/json
Cookie: access_token=...; refresh_token=...

{
  "is_active": null
}

# Ответ (200):
{
  "teachers": [
    {"id": 1, "fio": "Иванов Иван Иванович", "is_active": true},
    {"id": 2, "fio": "Петрова Анна Сергеевна", "is_active": true},
    {"id": 10, "fio": "Смирнов Олег Петрович", "is_active": false}
  ]
}
```

### ➕ Добавление нового преподавателя

```python
POST /api/v1/private/teachers/add
Content-Type: application/json
Cookie: access_token=...; refresh_token=...

{
  "fio": "Кузнецова Мария Александровна"
}

# Ответ (200):
{
  "success": true,
  "message": "Учитель добавлен",
  "teacher_id": 42
}
```

### 🔄 Массовое обновление статусов

```python
PUT /api/v1/private/teachers/update
Content-Type: application/json
Cookie: access_token=...; refresh_token=...

{
  "set_as_active": [15, 16],
  "set_as_deprecated": [1, 2, 3]
}

# Ответ (200):
{
  "success": true,
  "message": "Учителя сменили статусы",
  "active_upd_count": 2,
  "depr_upd_count": 3
}
```

### ⚠️ Обработка ошибок

```python
# Попытка добавить существующего преподавателя
POST /api/v1/private/teachers/add
Content-Type: application/json
Cookie: access_token=...; refresh_token=...

{
  "fio": "Иванов Иван Иванович"
}

# Ответ (409):
{
  "success": false,
  "message": "Такой учитель уже добавлен",
  "detail": "Такой учитель уже добавлен"
}
```

### 📄 Пагинация

```python
# Первая страница (20 записей)
POST /api/v1/private/teachers/get?limit=20&offset=0
{"is_active": true}

# Вторая страница
POST /api/v1/private/teachers/get?limit=20&offset=20
{"is_active": true}

# Третья страница
POST /api/v1/private/teachers/get?limit=20&offset=40
{"is_active": true}
```

## 🔄 Типичный workflow

### Добавление нового преподавателя

```
1. Методист добавляет преподавателя:
   POST /api/v1/private/teachers/add
   {"fio": "Кузнецова Мария Александровна"}
   → teacher_id: 42

2. Преподаватель становится доступен для назначения в расписании:
   - Методист создаёт карточку расписания (ext_card.py)
   - Выбирает преподавателя из списка
   - Преподаватель назначается на урок

3. Преподаватель отображается в расписании студентов
```

### Деактивация уволившегося преподавателя

```
1. Методист просматривает активных преподавателей:
   POST /api/v1/private/teachers/get
   {"is_active": true}

2. Методист деактивирует уволившегося преподавателя:
   PUT /api/v1/private/teachers/update
   {"set_as_active": [], "set_as_deprecated": [15]}

3. Преподаватель скрыт из интерфейса, но сохранён в истории расписания
```

## 🔗 Связь с другими модулями

### ext_card

Преподаватели назначаются на уроки через карточки:
- Методист создаёт/редактирует карточку расписания
- Выбирает преподавателя из списка активных
- Преподаватель привязывается к карточке
- См. `docs/api/n8n_ui_ext_card.md`

### timetable_api

Студенты видят преподавателей в расписании:
- Публичный API возвращает ФИО преподавателя
- Отображается в расписании группы
- См. `docs/api/timetable_api.md`

### main_ui

Преподаватели используются при создании версий расписания:
- Методист создаёт версию расписания
- Назначает преподавателей на уроки
- Проверяет конфликты (один преподаватель в разных группах одновременно)
- См. `docs/api/n8n_ui_main_ui.md`
