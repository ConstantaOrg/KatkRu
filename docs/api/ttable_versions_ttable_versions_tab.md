# 📚 Timetable Versions Management API

## Описание
API для управления версиями расписания. Позволяет методистам просматривать, фильтровать версии расписания и импортировать новые версии из DOCX файлов. Все операции изолированы по корпусам учебного заведения через `building_id`.

## Эндпоинты

### 📋 POST `/api/v1/private/ttable/versions/get_all`
Получение списка версий расписания с фильтрацией и пагинацией.

**Требования:**
- Роль: `methodist` или `read_all`
- JWT токен в cookie

**Тело запроса:**
```json
{
  "type": "standard",
  "status_id": 1,
  "schedule_date": "2024-03-15",
  "is_commited": true,
  "date_sort": "desc",
  "limit": 20,
  "offset": 0
}
```

**Параметры:**
- `type` (Literal["standard", "replacements"] | null) - тип расписания:
  - `"standard"` - стандартное расписание (шаблон)
  - `"replacements"` - замены на конкретную дату
  - `null` - все типы
- `status_id` (int | null) - статус версии:
  - `1` - утверждено (accepted)
  - `2` - в ожидании (pending)
  - `null` - все статусы
- `schedule_date` (date | null) - дата расписания (для replacements)
- `is_commited` (bool | null) - флаг утверждённой версии:
  - `true` - только утверждённые (продовые)
  - `false` - только неутверждённые
  - `null` - все версии
- `date_sort` (Literal["asc", "desc"], default="desc") - сортировка по дате создания
- `limit` (int, default=10) - количество записей
- `offset` (int, default=0) - смещение для пагинации

**Ответ 200:**
```json
{
  "ttable_versions": [
    {
      "id": 42,
      "type": "standard",
      "status_id": 1,
      "schedule_date": null,
      "is_commited": true,
      "created_at": "2024-03-15T10:30:00",
      "updated_at": "2024-03-15T11:00:00",
      "created_by": 5
    },
    {
      "id": 43,
      "type": "replacements",
      "status_id": 1,
      "schedule_date": "2024-03-20",
      "is_commited": true,
      "created_at": "2024-03-16T09:00:00",
      "updated_at": "2024-03-16T09:30:00",
      "created_by": 5
    }
  ]
}
```

**Поля ответа:**
- `id` (int) - ID версии расписания
- `type` (str) - тип расписания
- `status_id` (int) - статус версии
- `schedule_date` (date | null) - дата расписания (для replacements)
- `is_commited` (bool) - флаг утверждённой версии
- `created_at` (datetime) - дата создания
- `updated_at` (datetime) - дата обновления
- `created_by` (int) - ID пользователя-создателя

**Особенности:**
- Возвращаются только версии для корпуса методиста (`building_id` из JWT)
- Поле `building_id` не включается в ответ
- Логируется для аудита и анализа

---

### 🔍 POST `/api/v1/private/ttable/versions/get_by_id`
Получение конкретной версии расписания по ID.

**Требования:**
- Роль: `methodist` или `read_all`
- JWT токен в cookie

**Тело запроса:**
```json
{
  "ttable_id": 42
}
```

**Параметры:**
- `ttable_id` (int) - ID версии расписания

**Ответ 200:**
```json
{
  "ttable_version": {
    "id": 42,
    "type": "standard",
    "status_id": 1,
    "schedule_date": null,
    "is_commited": true,
    "created_at": "2024-03-15T10:30:00",
    "updated_at": "2024-03-15T11:00:00",
    "created_by": 5
  }
}
```

**Особенности:**
- Проверяется принадлежность версии к корпусу методиста
- Если версия не найдена или принадлежит другому корпусу, возвращается `null`
- Логируется ID версии, user_id и building_id для аудита

---

### 🔄 POST `/api/v1/private/ttable/versions/replace/get_candidates`
Получение списка версий-кандидатов для замены текущей утверждённой версии.

**Требования:**
- Роль: `methodist` или `read_all`
- JWT токен в cookie

**Тело запроса:**
```json
{
  "ttable_id": 42
}
```

**Параметры:**
- `ttable_id` (int) - ID текущей версии расписания

**Ответ 200:**
```json
{
  "ttable_candidates": [
    {
      "id": 45,
      "type": "standard",
      "status_id": 1,
      "schedule_date": null,
      "is_commited": false,
      "created_at": "2024-03-16T10:00:00",
      "updated_at": "2024-03-16T11:00:00",
      "created_by": 5
    },
    {
      "id": 46,
      "type": "standard",
      "status_id": 1,
      "schedule_date": null,
      "is_commited": false,
      "created_at": "2024-03-17T09:00:00",
      "updated_at": "2024-03-17T10:00:00",
      "created_by": 7
    }
  ]
}
```

**Критерии отбора кандидатов:**
- Совпадают по `type` с исходной версией
- Совпадают по `schedule_date` с исходной версией
- Совпадают по `building_id` (корпус)
- Имеют `is_commited = false` (не утверждены)
- Имеют `status_id = 1` (утверждены, но не "продовые")

**Особенности:**
- Используется для выбора версии, которую можно установить вместо текущей
- Логируется количество найденных кандидатов для мониторинга

---

### 📥 POST `/api/v1/private/ttable/versions/standard/import`
Импорт стандартного расписания из DOCX файла.

**Требования:**
- Роль: `methodist` (только методисты могут импортировать)
- JWT токен в cookie

**Параметры запроса:**
- `file_obj` (UploadFile) - DOCX файл с расписанием
- `smtr` (query, Literal["1", "2"]) - номер семестра:
  - `"1"` - первый семестр (числитель)
  - `"2"` - второй семестр (знаменатель)

**Пример запроса (multipart/form-data):**
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/standard/import?smtr=1" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -F "file_obj=@schedule.docx"
```

**Ответ 200:**
```json
{
  "success": true,
  "message": "Расписание сохранено",
  "ttable_ver_id": 47,
  "status": "В Ожидании"
}
```

**Поля ответа:**
- `success` (bool) - успешность операции
- `message` (str) - сообщение о результате
- `ttable_ver_id` (int) - ID созданной версии расписания
- `status` (str) - статус версии ("В Ожидании")

**Процесс импорта:**

1. **Парсинг DOCX файла:**
   - Вызывается `std_ttable_doc_processer()` из `ttable_parser.py`
   - Извлекаются данные: группы, дисциплины, преподаватели, аудитории
   - Обрабатывается числитель/знаменатель в зависимости от `semester`

2. **Нормализация данных:**
   - Текстовые значения преобразуются в ID из БД
   - Создаются кортежи для вставки в `card_history`

3. **Сохранение в БД:**
   - Создаётся новая версия расписания со статусом "В Ожидании" (pending)
   - Вставляются все уроки из распарсенного файла
   - Версия привязывается к корпусу методиста (`building_id`)
   - Сохраняется `user_id` создателя

4. **Возврат результата:**
   - Возвращается ID созданной версии
   - Статус "В Ожидании" означает, что версия требует проверки и утверждения

**Особенности:**
- Импорт не гарантирует корректность данных - требуется проверка методистом
- После импорта версия имеет `status_id=2` (pending) и `is_commited=false`
- Методист должен проверить расписание в UI и утвердить через другой эндпоинт
- Логируется имя файла и процесс обработки

## Бизнес-логика

### Типы расписания

**Standard (стандартное):**
- Шаблон расписания, действующий по умолчанию
- Не привязано к конкретной дате (`schedule_date = null`)
- Используется, если нет замен на текущую дату
- Может быть только одна утверждённая версия с `is_commited=true` для каждого корпуса

**Replacements (замены):**
- Расписание на конкретную дату
- Привязано к дате (`schedule_date` указана)
- Используется вместо стандартного на указанную дату
- Может быть только одна утверждённая версия с `is_commited=true` для каждой даты и корпуса

### Статусы версий

Статусы определены в `core.utils.anything.TimetableVerStatuses`:

1. **Accepted (утверждено)** - `status_id = 1`
   - Версия прошла проверку и готова к использованию
   - Может быть утверждена (`is_commited=true`) или нет

2. **Pending (в ожидании)** - `status_id = 2`
   - Версия создана, но требует проверки
   - Всегда имеет `is_commited=false`
   - Устанавливается при импорте из файла

### Флаг is_commited

**is_commited = true (утверждённая, "продовая"):**
- Версия активна и используется в приложении
- Отображается студентам и преподавателям
- Может быть только одна такая версия для каждой комбинации:
  - `building_id + type="standard"` (одно стандартное расписание на корпус)
  - `building_id + type="replacements" + schedule_date` (одни замены на дату)

**is_commited = false (неутверждённая):**
- Версия в разработке или архивная
- Не отображается пользователям
- Может быть несколько таких версий

### Изоляция по корпусам

Все операции изолированы по `building_id`:
- Методист видит только версии своего корпуса
- `building_id` извлекается из JWT токена (`request.state.building_id`)
- Это позволяет методистам разных корпусов работать независимо
- Всего корпусов: 3 (указано в документации к другим модулям)

### Процесс работы с версиями

**Типичный workflow:**

1. **Импорт нового расписания:**
   - Методист загружает DOCX файл через `/standard/import`
   - Создаётся версия со статусом "В Ожидании" (`status_id=2, is_commited=false`)

2. **Проверка и редактирование:**
   - Методист открывает версию в UI редакторе
   - Проверяет корректность данных
   - Вносит правки при необходимости

3. **Утверждение версии:**
   - Методист утверждает версию (через другой эндпоинт, не в этом модуле)
   - Статус меняется на "Утверждено" (`status_id=1`)

4. **Публикация (commit):**
   - Методист публикует версию (через эндпоинт commit)
   - Устанавливается `is_commited=true`
   - Предыдущая утверждённая версия получает `is_commited=false`
   - Новая версия становится активной

### Замена версий

Эндпоинт `/replace/get_candidates` используется для:
- Получения списка версий, которые можно установить вместо текущей
- Выбора версии для отката к предыдущему состоянию
- Переключения между разными вариантами расписания

**Пример сценария:**
- Текущая версия: standard, is_commited=true
- Кандидаты: другие standard версии с is_commited=false и status_id=1
- Методист выбирает кандидата и делает commit
- Кандидат становится активным, текущая версия архивируется

## Зависимости

### Внутренние модули
- `core.api.ttable_versions.ttable_parser` - парсинг DOCX файлов
- `core.data.postgre.PgSqlDep` - dependency injection для PostgreSQL
- `core.schemas.cookie_settings_schema.JWTCookieDep` - валидация JWT токена
- `core.schemas.ttable_schema` - схемы запросов
- `core.response_schemas.timetable_api.TimetableImportResponse` - схема ответа импорта
- `core.utils.anything.Roles` - роли пользователей
- `core.utils.lite_dependencies.role_require` - проверка ролей
- `core.utils.logger.log_event` - логирование операций

### База данных

**Таблица:** `ttable_versions`

**Поля:**
- `id` - первичный ключ
- `type` - тип расписания (standard/replacements)
- `status_id` - статус версии (1=accepted, 2=pending)
- `schedule_date` - дата расписания (для replacements)
- `is_commited` - флаг утверждённой версии
- `building_id` - ID корпуса (для изоляции)
- `created_at` - дата создания
- `updated_at` - дата обновления
- `created_by` - ID пользователя-создателя

**Методы репозитория:**
- `filtered_layout()` - получение версий с фильтрацией
- `get_by_id()` - получение версии по ID
- `get_candidates_by_ver()` - получение кандидатов для замены
- `import_raw_std_ttable()` - импорт распарсенного расписания

## Примеры использования

### Получение всех утверждённых версий
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/get_all" \
  -H "Content-Type: application/json" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -d '{
    "status_id": 1,
    "is_commited": true,
    "date_sort": "desc",
    "limit": 20,
    "offset": 0
  }'
```

### Получение версий в ожидании
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/get_all" \
  -H "Content-Type: application/json" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -d '{
    "status_id": 2,
    "date_sort": "desc"
  }'
```

### Получение версии по ID
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/get_by_id" \
  -H "Content-Type: application/json" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -d '{
    "ttable_id": 42
  }'
```

### Получение кандидатов для замены
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/replace/get_candidates" \
  -H "Content-Type: application/json" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -d '{
    "ttable_id": 42
  }'
```

### Импорт расписания (числитель)
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/standard/import?smtr=1" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -F "file_obj=@schedule_semester1.docx"
```

### Импорт расписания (знаменатель)
```bash
curl -X POST "http://localhost:8000/api/v1/private/ttable/versions/standard/import?smtr=2" \
  -H "Cookie: access_token=<JWT_TOKEN>" \
  -F "file_obj=@schedule_semester2.docx"
```

## Интеграция с другими модулями

### Связь с ttable_parser.py
Модуль использует парсер для импорта:
- `std_ttable_doc_processer()` - парсинг DOCX файла
- `raw_values2db_ids_handler()` - преобразование в ID (вызывается в репозитории)

### Связь с timetable_api.py
После утверждения версии:
- Публичный API `timetable_api.py` использует утверждённые версии (`is_commited=true`)
- Студенты и преподаватели видят только активные версии
- Методисты управляют версиями через этот модуль

### Связь с аутентификацией
Все эндпоинты используют:
- `JWTCookieDep` для валидации токена
- `role_require()` для проверки прав доступа
- `request.state.building_id` для изоляции по корпусам
- `request.state.user_id` для аудита действий

### Связь с логированием
Все операции логируются:
- Фильтры и количество результатов
- ID версий и пользователей
- Имена импортируемых файлов
- Логи отправляются в Elasticsearch для мониторинга

## Типичные сценарии

1. **Просмотр всех версий расписания:**
   - Методист открывает раздел "Версии расписания" в UI
   - Фронтенд вызывает `/get_all` без фильтров
   - Отображается список всех версий с пагинацией

2. **Фильтрация по статусу:**
   - Методист выбирает "Показать только в ожидании"
   - Фронтенд вызывает `/get_all` с `status_id=2`
   - Отображаются только версии, требующие проверки

3. **Импорт нового расписания:**
   - Методист загружает DOCX файл
   - Выбирает семестр (числитель/знаменатель)
   - Система парсит файл и создаёт версию со статусом "В Ожидании"
   - Методист получает ID версии для дальнейшей работы

4. **Проверка импортированной версии:**
   - Методист получает ID версии после импорта
   - Вызывает `/get_by_id` для получения деталей
   - Открывает версию в редакторе для проверки

5. **Выбор версии для замены:**
   - Методист хочет откатиться к предыдущей версии
   - Вызывает `/replace/get_candidates` для текущей версии
   - Получает список доступных версий
   - Выбирает нужную и делает commit (через другой эндпоинт)

## Безопасность

### Изоляция данных
- Все запросы фильтруются по `building_id` из JWT токена
- Методист не может видеть/изменять версии других корпусов
- `building_id` не возвращается в ответах (скрыт от клиента)

### Контроль доступа
- Только роли `methodist` и `read_all` имеют доступ
- Импорт доступен только `methodist` (не `read_all`)
- JWT токен обязателен для всех эндпоинтов

### Аудит
- Все операции логируются с `user_id`
- Логи содержат детали запросов и результатов
- Позволяет отследить, кто создал/изменил версию

## Ограничения

1. **Импорт не гарантирует корректность:**
   - Парсер может неправильно распознать данные из DOCX
   - Требуется ручная проверка методистом после импорта

2. **Формат DOCX должен соответствовать:**
   - Парсер рассчитан на определённую структуру таблицы
   - Изменения в формате могут сломать импорт

3. **Нет автоматической валидации:**
   - Не проверяется наличие всех сущностей в БД перед импортом
   - Ошибки обнаруживаются только при попытке сохранения

## Будущие улучшения

1. **Предварительная валидация:**
   - Проверка файла перед импортом
   - Отчёт о недостающих сущностях (преподаватели, дисциплины, группы)

2. **Превью импорта:**
   - Показать распарсенные данные перед сохранением
   - Позволить исправить ошибки до создания версии

3. **История изменений:**
   - Отслеживание изменений между версиями
   - Diff для сравнения версий

4. **Автоматическое утверждение:**
   - Опция для автоматического утверждения при импорте
   - Для доверенных источников данных

## Теги
`Ttable Versions` - группировка в Swagger UI для удобной навигации по документации API.
