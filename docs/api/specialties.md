# 📖 Specialties API

## Описание
Публичный API для работы со специальностями учебного заведения. Предоставляет информацию о доступных специальностях для абитуриентов и посетителей сайта.

## Эндпоинты

### 📋 POST `/api/v1/public/specialties/all`
Получение списка всех специальностей с пагинацией.

**Тело запроса:**
```json
{
  "limit": 6,
  "offset": 0
}
```

**Параметры:**
- `limit` (int, default=6, >=0) - количество записей для выборки
- `offset` (int, default=0, >=0) - смещение для пагинации

**Ответ 200:**
```json
{
  "specialties": [
    {
      "learn_years": 3,
      "title": "Информационные системы и программирование"
    },
    {
      "learn_years": 4,
      "title": "Прикладная информатика"
    }
  ]
}
```

**Поля ответа:**
- `learn_years` (int) - количество лет обучения
- `title` (str) - название специальности

---

### 🔍 GET `/api/v1/public/specialties/{spec_id}`
Получение детальной информации об одной специальности.

**Параметры пути:**
- `spec_id` (int) - идентификатор специальности

**Ответ 200:**
```json
{
  "speciality": {
    "spec_code": "09.02.07",
    "description": "Подготовка специалистов в области разработки программного обеспечения",
    "full_time": true,
    "free_form": true,
    "evening_form": false,
    "cost_per_year": 120000
  }
}
```

**Поля ответа:**
- `spec_code` (str) - код специальности по ФГОС
- `description` (str) - описание специальности
- `full_time` (bool) - доступность очной формы обучения
- `free_form` (bool) - доступность бесплатной формы обучения
- `evening_form` (bool) - доступность вечерней формы обучения
- `cost_per_year` (int) - стоимость обучения в год (рубли)

## Бизнес-логика

### Публичный доступ
Модуль использует префикс `/public/`, что означает открытый доступ без аутентификации. Это позволяет абитуриентам и посетителям сайта просматривать информацию о специальностях.

### Пагинация
Список специальностей поддерживает пагинацию через параметры `limit` и `offset`:
- По умолчанию возвращается 6 записей (`limit=6`)
- Для получения следующей страницы увеличивайте `offset` на значение `limit`

### Формы обучения
Поля `full_time`, `free_form`, `evening_form` описывают свойства специальности как понятия предметной области. Они информируют абитуриентов о доступных формах обучения.

## Зависимости

### Внутренние модули
- `core.data.postgre.PgSqlDep` - dependency injection для подключения к PostgreSQL
- `core.schemas.specs_schema.SpecsPaginSchema` - схема пагинации
- `core.response_schemas.specialties` - схемы ответов API
- `core.data.sql_queries.specialties_sql.SpecsQueries` - репозиторий для работы с БД

### База данных
**Таблица:** `specialties`

**Используемые поля:**
- `id` - первичный ключ
- `learn_years` - количество лет обучения
- `title` - название специальности
- `spec_code` - код специальности
- `description` - описание
- `full_time` - очная форма
- `free_form` - бесплатная форма
- `evening_form` - вечерняя форма
- `cost_per_year` - стоимость в год

## Примеры использования

### Получение первой страницы специальностей
```bash
curl -X POST "http://localhost:8000/api/v1/public/specialties/all" \
  -H "Content-Type: application/json" \
  -d '{"limit": 6, "offset": 0}'
```

### Получение второй страницы
```bash
curl -X POST "http://localhost:8000/api/v1/public/specialties/all" \
  -H "Content-Type: application/json" \
  -d '{"limit": 6, "offset": 6}'
```

### Получение детальной информации
```bash
curl -X GET "http://localhost:8000/api/v1/public/specialties/1"
```

## Интеграция с другими модулями

### Elasticsearch (будущая интеграция)
Метод `specialties2elastic()` в репозитории подготавливает данные для индексации специальностей в Elasticsearch. Это будет использоваться для реализации поиска по специальностям (см. задачу 10.1).

**Индексируемые поля:**
- `id` - идентификатор
- `spec_code` - код специальности
- `title` - название

### Связь с расписанием
Специальности связаны с группами студентов, для которых составляется расписание. Каждая группа обучается по определённой специальности.

## Типичные сценарии

1. **Просмотр каталога специальностей:**
   - Абитуриент открывает страницу со списком специальностей
   - Фронтенд запрашивает первую страницу через `POST /all`
   - При прокрутке загружаются следующие страницы

2. **Детальная информация:**
   - Пользователь кликает на специальность
   - Фронтенд запрашивает детали через `GET /{spec_id}`
   - Отображается полная информация: код, описание, формы обучения, стоимость

## Теги
`Specs📖` - группировка в Swagger UI для удобной навигации по документации API.
