# 📄 Timetable Parser

## Описание
Модуль для парсинга расписания из документов Word (.docx) и преобразования данных для загрузки в базу данных. Используется при импорте расписания из файла как новой версии в цифровом формате, которую можно редактировать в UI.

## Функции

### 📖 `std_ttable_doc_processer()`
Парсинг DOCX файла с расписанием и извлечение структурированных данных.

**Сигнатура:**
```python
def std_ttable_doc_processer(
    doc_file,
    semester: Literal[1, 2]
) -> dict
```

**Параметры:**
- `doc_file` - файловый объект DOCX документа
- `semester` (Literal[1, 2]) - номер семестра:
  - `1` - первый семестр (числитель) - берётся первое вхождение пары
  - `2` - второй семестр (знаменатель) - берётся второе вхождение пары

**Возвращает:**
- `dict` - структура расписания:
```python
{
    "23И1": {
        "понедельник": {
            1: {
                "discipline": "Математика",
                "auditory": "201, 202",
                "teachers": ["Иванов И.И.", "Петров П.П."]
            },
            2: {...}
        },
        "вторник": {...}
    },
    "23И2": {...}
}
```

**Логика работы:**

1. **Парсинг заголовка таблицы:**
   - Извлекает названия групп из первой строки
   - Группы расположены каждые 3 колонки начиная с индекса 2
   - Структура: `[День, Номер пары, Группа1, Аудитория1, Номер пары, Группа2, Аудитория2, ...]`

2. **Обработка строк с данными:**
   - Первая колонка содержит день недели (обновляется при наличии)
   - Для каждой группы извлекаются: предмет, аудитория, номер пары

3. **Обработка семестров (числитель/знаменатель):**
   - Если пара с таким номером уже существует и `semester=1`, пропускается (берётся числитель)
   - Если `semester=2`, перезаписывается (берётся знаменатель)
   - Это позволяет обрабатывать расписания с чередованием по неделям

4. **Извлечение преподавателей:**
   - Сначала ищет ФИО в отдельной строке (после названия дисциплины)
   - Если не найдено, ищет во всём тексте ячейки
   - При нахождении в тексте, удаляет ФИО из названия дисциплины

5. **Нормализация данных:**
   - Название группы приводится к верхнему регистру
   - Аудитории объединяются через запятую (если несколько)
   - Лишние пробелы удаляются из названия дисциплины

---

### 🔄 `raw_values2db_ids_handler()`
Преобразование текстовых значений (названия, ФИО) в ID из базы данных.

**Сигнатура:**
```python
async def raw_values2db_ids_handler(
    std_ttable: dict,
    sched_ver_id: int,
    conn_obj
) -> list[tuple]
```

**Параметры:**
- `std_ttable` (dict) - результат работы `std_ttable_doc_processer()`
- `sched_ver_id` (int) - ID версии расписания в БД
- `conn_obj` - объект `TimetableQueries` для работы с БД

**Возвращает:**
- `list[tuple]` - список кортежей для вставки в БД:
```python
[
    (sched_ver_id, group_id, discipline_id, position, auditory, teacher_id, day_index),
    (1, 5, 12, 1, "201", 3, 0),  # Пример
    ...
]
```

**Логика работы:**

1. **Загрузка справочников из БД:**
   - Получает все дисциплины: `{title: id}`
   - Получает всех преподавателей: `{fio: id}`
   - Получает все группы: `{name: id}`

2. **Преобразование текстовых значений в ID:**
   - Названия дисциплин → `discipline_id`
   - ФИО преподавателей → `teacher_id`
   - Названия групп → `group_id`

3. **Распаковка структуры расписания:**
   - Проходит по всем группам, дням недели и парам
   - Преобразует день недели в индекс (0-5)
   - Преобразует номер пары в позицию

4. **Обработка множественных преподавателей:**
   - Если у пары несколько преподавателей, создаётся отдельная запись для каждого
   - Каждому преподавателю соответствует своя аудитория из списка через запятую
   - Пример: "201, 202" → преподаватель 1 в 201, преподаватель 2 в 202

5. **Формирование кортежей для вставки:**
   - Каждый кортеж содержит все данные для одной записи в таблице `card_history`
   - Готов для массовой вставки через `executemany()`

---

### 🔤 `normalize_day_name()`
Нормализация названия дня недели.

**Сигнатура:**
```python
def normalize_day_name(day: str) -> str
```

**Параметры:**
- `day` (str) - название дня (например, "Понедельник.", "ПН", "Вторник")

**Возвращает:**
- `str` - нормализованное название в нижнем регистре без точек

**Примеры:**
```python
normalize_day_name("Понедельник.") # "понедельник"
normalize_day_name("ПН") # "пн"
normalize_day_name("  Вторник  ") # "вторник"
```

---

### 🔢 `day_to_index()`
Преобразование названия дня недели в числовой индекс.

**Сигнатура:**
```python
def day_to_index(day: str) -> int
```

**Параметры:**
- `day` (str) - название дня недели

**Возвращает:**
- `int` - индекс дня (0-5):
  - 0 - понедельник
  - 1 - вторник
  - 2 - среда
  - 3 - четверг
  - 4 - пятница
  - 5 - суббота

**Исключения:**
- `ValueError` - если день недели не распознан

**Поддерживаемые форматы:**
- Полные названия: "понедельник", "вторник", "среда", "четверг", "пятница", "суббота"
- Сокращения: "пн", "вт", "ср", "чт", "пт", "сб"
- Опечатки: "суббта" (без 'о') - специально для обработки опечаток в документах

---

### 👨‍🏫 `extract_teachers()`
Извлечение ФИО преподавателей из текста с помощью регулярного выражения.

**Сигнатура:**
```python
def extract_teachers(text: str) -> list[str]
```

**Параметры:**
- `text` (str) - текст для поиска ФИО

**Возвращает:**
- `list[str]` - список ФИО в формате "Фамилия И.О."

**Паттерн поиска:**
- Регулярное выражение: `([А-ЯЁ][а-яё]+)\s+([А-ЯЁ]\.[А-ЯЁ]\.)`
- Ищет: Фамилия (с заглавной буквы) + пробел + Инициалы (И.О.)

**Примеры:**
```python
extract_teachers("Математика Иванов И.И.")
# ["Иванов И.И."]

extract_teachers("Физика Петров П.П. Сидоров С.С.")
# ["Петров П.П.", "Сидоров С.С."]

extract_teachers("Математика")
# []
```

## Бизнес-логика

### Формат DOCX документа

**Структура таблицы:**

| День недели | № пары | Группа 23И1 | Ауд. | № пары | Группа 23И2 | Ауд. | ... |
|-------------|--------|-------------|------|--------|-------------|------|-----|
| Понедельник | 1      | Математика<br>Иванов И.И. | 201  | 1      | Физика<br>Петров П.П. | 301  | ... |
|             | 2      | Информатика<br>Сидоров С.С. | 202  | 2      | Химия<br>Козлов К.К. | 302  | ... |
| Вторник     | 1      | ...         | ...  | ...    | ...         | ...  | ... |

**Особенности формата:**
- Первая строка - заголовки с названиями групп
- Группы повторяются каждые 3 колонки (номер пары, группа, аудитория)
- День недели указывается в первой колонке (может быть пустым для продолжения дня)
- Предмет и преподаватель разделены переносом строки в ячейке
- Нет чёткой структуры данных - иногда формат нарушается

### Обработка семестров (числитель/знаменатель)

В некоторых учебных заведениях расписание чередуется по неделям:
- **Числитель** (нечётные недели) - первый набор пар
- **Знаменатель** (чётные недели) - второй набор пар

**Логика обработки:**
- При `semester=1`: если пара с таким номером уже встречалась, пропускается (берётся первое вхождение)
- При `semester=2`: пара перезаписывается (берётся второе вхождение)

**Пример:**
```
Понедельник, пара 1:
- Первое вхождение: Математика (числитель)
- Второе вхождение: Физика (знаменатель)

semester=1 → Математика
semester=2 → Физика
```

### Извлечение преподавателей

**Стратегия извлечения:**

1. **Явная строка преподавателя:**
   - Если в ячейке есть перенос строки, вторая строка считается ФИО
   - Пример: "Математика\nИванов И.И."

2. **Fallback - поиск во всём тексте:**
   - Если явной строки нет, ищется паттерн "Фамилия И.О." во всём тексте
   - ФИО удаляется из названия дисциплины
   - Пример: "Математика Иванов И.И." → дисциплина="Математика", учитель="Иванов И.И."

**Обработка множественных преподавателей:**
- Если найдено несколько ФИО, все добавляются в список
- Каждому преподавателю соответствует своя аудитория из списка через запятую
- Создаётся отдельная запись в БД для каждого преподавателя

### Нормализация данных

**Группы:**
- Приводятся к верхнему регистру: "23и1" → "23И1"
- Используется для единообразия в БД

**Дни недели:**
- Удаляются точки и пробелы
- Приводятся к нижнему регистру
- Обрабатываются опечатки ("суббта")

**Дисциплины:**
- Удаляются двойные пробелы
- Удаляются ФИО преподавателей (если найдены в тексте)

**Аудитории:**
- Переносы строк заменяются на запятые
- Пример: "201\n202" → "201, 202"

## Зависимости

### Внутренние модули
- Нет прямых зависимостей от других модулей приложения
- Используется в `ttable_versions_tab.py` для импорта расписания

### Внешние зависимости
- `python-docx` - библиотека для работы с DOCX файлами
- `re` - регулярные выражения для извлечения ФИО
- `collections.defaultdict` - для удобной работы с вложенными словарями

### База данных

**Используемые методы `TimetableQueries`:**
- `discipline_ids()` - получение всех дисциплин `{title: id}`
- `teacher_ids()` - получение всех преподавателей `{fio: id}`
- `group_ids()` - получение всех групп `{name: id}`

## Пример документа для импорта

### 📥 Шаблон DOCX файла

Для корректного импорта расписания, DOCX документ должен соответствовать определённой структуре.

**Ссылка на пример документа:**
> 🔗 **[Скачать пример документа расписания](ССЫЛКА_НА_S3_BUCKET)**
> 
> ⚠️ *Примечание: Ссылка будет добавлена после настройки S3 бакета*

**Требования к формату:**
- Файл должен содержать таблицу с расписанием
- Первая строка - заголовки с названиями групп
- Структура колонок: День недели | № пары | Группа | Аудитория | № пары | Группа | Аудитория | ...
- Предмет и ФИО преподавателя разделены переносом строки в ячейке
- Дни недели: понедельник, вторник, среда, четверг, пятница, суббота (или сокращения)

## Примеры использования

### Парсинг DOCX файла
```python
from core.api.ttable_versions.ttable_parser import std_ttable_doc_processer

# Открываем файл
with open("schedule.docx", "rb") as f:
    # Парсим расписание для первого семестра (числитель)
    schedule = std_ttable_doc_processer(f, semester=1)

# Результат
print(schedule["23И1"]["понедельник"][1])
# {
#     "discipline": "Математика",
#     "auditory": "201",
#     "teachers": ["Иванов И.И."]
# }
```

### Преобразование в ID для БД
```python
from core.api.ttable_versions.ttable_parser import raw_values2db_ids_handler
from core.data.postgre import PgSql

# Получаем распарсенное расписание
schedule = std_ttable_doc_processer(doc_file, semester=1)

# Преобразуем в кортежи для вставки
async with db.acquire() as conn:
    conn = PgSql(conn)
    lessons = await raw_values2db_ids_handler(
        schedule,
        sched_ver_id=42,
        conn_obj=conn.timetable
    )

# Результат - список кортежей
print(lessons[0])
# (42, 5, 12, 1, "201", 3, 0)
# (sched_ver_id, group_id, discipline_id, position, auditory, teacher_id, day_index)
```

### Полный цикл импорта
```python
# В эндпоинте импорта расписания
@router.post("/import")
async def import_schedule(file: UploadFile, semester: int, db: PgSqlDep):
    # 1. Парсим DOCX
    schedule = std_ttable_doc_processer(file.file, semester=semester)
    
    # 2. Создаём новую версию расписания в БД
    sched_ver_id = await db.timetable.create_version(...)
    
    # 3. Преобразуем в ID
    lessons = await raw_values2db_ids_handler(schedule, sched_ver_id, db.timetable)
    
    # 4. Массовая вставка в БД
    await db.timetable.insert_lessons(lessons)
    
    return {"success": True, "lessons_count": len(lessons)}
```

## Интеграция с другими модулями

### Связь с ttable_versions_tab.py
Функции парсера используются в эндпоинте импорта расписания:
- Методист загружает DOCX файл через UI
- Эндпоинт вызывает `std_ttable_doc_processer()` для парсинга
- Затем `raw_values2db_ids_handler()` преобразует данные
- Результат вставляется в БД как новая версия расписания

### Связь с базой данных
Модуль работает с таблицами:
- `specialties` - для получения ID дисциплин
- `teachers` - для получения ID преподавателей
- `groups` - для получения ID групп
- `card_history` - для вставки распарсенных уроков

## Типичные сценарии

1. **Импорт расписания на первый семестр:**
   - Методист загружает DOCX файл с расписанием
   - Выбирает semester=1 (числитель)
   - Система парсит файл и создаёт новую версию расписания
   - Расписание становится доступным для редактирования в UI

2. **Импорт расписания на второй семестр:**
   - Методист загружает тот же файл
   - Выбирает semester=2 (знаменатель)
   - Система берёт вторые вхождения пар (для чётных недель)
   - Создаётся отдельная версия расписания

3. **Обработка пары с несколькими преподавателями:**
   - В DOCX: "Математика Иванов И.И. Петров П.П.", аудитории "201, 202"
   - Парсер извлекает двух преподавателей
   - Создаются две записи в БД:
     - Иванов И.И. в аудитории 201
     - Петров П.П. в аудитории 202

4. **Обработка опечаток в документе:**
   - В DOCX написано "Суббта" вместо "Суббота"
   - `normalize_day_name()` приводит к "суббта"
   - `day_to_index()` распознаёт как субботу (индекс 5)
   - Данные корректно сохраняются

## Обработка ошибок

### Возможные проблемы

1. **Неизвестный день недели:**
   - `day_to_index()` выбрасывает `ValueError`
   - Необходимо добавить день в словарь `day_weeks`

2. **ФИО не найдено в БД:**
   - `t2id[t]` выбросит `KeyError`
   - Преподаватель должен быть добавлен в БД перед импортом

3. **Дисциплина не найдена в БД:**
   - `disp2id[discipline]` выбросит `KeyError`
   - Дисциплина должна существовать в справочнике

4. **Группа не найдена в БД:**
   - `gr2id[group]` выбросит `KeyError`
   - Группа должна быть создана перед импортом

### Рекомендации

- Перед импортом проверять наличие всех сущностей в БД
- Добавить валидацию и информативные сообщения об ошибках
- Логировать проблемные записи для ручной обработки

## Ограничения

1. **Нестрогий формат DOCX:**
   - Парсер рассчитан на определённую структуру таблицы
   - Изменения в формате могут сломать парсинг

2. **Regex для ФИО:**
   - Работает только для формата "Фамилия И.О."
   - Не поддерживает полные имена и отчества

3. **Соответствие аудиторий преподавателям:**
   - Предполагается, что количество аудиторий = количеству преподавателей
   - Если не совпадает, может быть IndexError

4. **Только 6 дней недели:**
   - Воскресенье не поддерживается
   - Учебная неделя: понедельник-суббота

## Будущие улучшения

1. **Валидация данных:**
   - Проверка существования сущностей в БД перед преобразованием
   - Информативные сообщения об отсутствующих записях

2. **Гибкий парсинг:**
   - Поддержка различных форматов DOCX
   - Автоопределение структуры таблицы

3. **Обработка ошибок:**
   - Try/except блоки с логированием проблемных записей
   - Частичный импорт при ошибках в отдельных записях

4. **Расширенное извлечение ФИО:**
   - Поддержка полных имён
   - Нечёткое сопоставление с БД (fuzzy matching)
